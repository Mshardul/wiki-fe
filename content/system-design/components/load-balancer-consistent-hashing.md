# Load Balancer - Consistent Hashing Deep-Dive

Ring mechanics, virtual node tuning, rebalancing impact, and bounded-load extensions now live on the dedicated algorithm page: **[Consistent Hashing](../algorithms/consistent-hashing.md)**. This file is kept only so existing links resolve; the [Load Balancer](./load-balancer.md) article links there directly for the algorithm, and covers only the LB-specific usage (which key to hash, when to prefer it over cookie-based stickiness) itself.
