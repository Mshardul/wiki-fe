# Bitmask DP

## Prerequisites

<!-- U9: the DS/algo this pattern leans on. [Title](./path.md) [Must read] - reason. -->

## Table of Contents

<!-- U10. -->

## What it is

<!-- U1: the pattern in one line + mental model. U13: spoken soundbite, marked as the takeaway. -->

## Recognition signals

<!-- PA1 (weight 3, the heart). THREE labeled parts, all concrete — any missing/vague caps the score at 5:
     (a) Trigger phrases — ≥2 literal quoted problem-statement snippets.
     (b) Structural cues — input shape + output property (n ≤ 20, "assign each element", "visit all nodes", subset enumeration).
     (c) Not to be confused with — name ≥1 neighbor pattern (general DP, backtracking) + the distinction in one sentence. -->

## How it works

<!-- PA2: the mechanic + a REAL diagram (bitmask as set representation, transition from subset to superset). -->

## Skeleton

<!-- PA3: reusable CLRS pseudocode + a Python template with a real signature, no syntax errors, and a `# your logic here` marker. -->

## Complexity

<!-- PA4: typical time/space of the pattern — O(2ⁿ · n) time, O(2ⁿ) space. -->

## Constraints & approach

<!-- PA10 (gated): input size → reach for this pattern (or not). n ≤ 20 is the canonical signal. State when the constraint pushes you OFF this pattern (n > 20 → exponential blows up). -->

## Variations

<!-- PA5 (advisory): common twists (TSP, minimum cost assignment, broken profile DP, subset-sum over subsets). -->

## CP-primitives

<!-- PA11 (gated): ≥2 contest-flavored tricks/variants (e.g. SOS DP — sum over subsets in O(2ⁿ · n); iterating over subsets of a mask in O(3ⁿ) total). Each with one-line "why for CP". -->

## Worked problems

<!-- PA6: 3–5 problems reusing the pattern, each with a 2–3 sentence approach sketch (not a full solution). Favor staples; note constraint ranges where they change the approach. -->

## Pitfalls

<!-- PA7: ≥2 common misapplications. -->

## First 30 seconds

<!-- PA9 (advisory): the 2–3 sentences said out loud on spotting the pattern. -->

## Related

<!-- PA8: cross-links to the DS/algo it leans on + sibling patterns. -->

## Practice problems

<!-- U6: ≥3 problems, each a WORKED entry (### subheading per problem): (1) full problem statement in 2–3 sentences + constraints where they matter; (2) approach/insight in prose; (3) short runnable Python solution; (4) time/space complexity. EVERY PROBLEM MUST EXERCISE A DISTINCT TECHNIQUE — no duplicates padding the count. -->
