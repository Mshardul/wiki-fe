# Wiki App - Task Board (Backlog)

| ID       | Entry Date | Summary (upto 7 words)                               | Type     | Component                 | Description (upto 30 words)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Status  | Impl. Date | Remarks (upto 30 words)                                                                                                                                                               | priority | story points |
| -------- | ---------- | ---------------------------------------------------- | -------- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------ |
| WIKI-046 | 2026-04-22 | Offline bulk download                                | feature  | sw \| settings            | List cached articles in settings. Add bulk download.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Backlog | -          | Requires path normalisation mapping to SW Cache keys.                                                                                                                                 | p2       | 8            |
| WIKI-299 | 2026-06-22 | Background sync - queue offline writes, flush on reconnect | feature  | sw \| storage             | Bookmark adds/removes and note saves made while offline are queued in localStorage and flushed to the backend when the connection is restored, using the Background Sync API where available. | Backlog | -          | storage.js: wrap backend write calls in a queue when `!navigator.onLine`; register a sync tag via SW on queue write. SW `sync` handler flushes the queue via `api.js`. Degrades silently on browsers without Background Sync. | p5       | 8            |
| WIKI-462 | 2026-07-18 | DSA Learning Paths: index section + completion button | feature  | render \| content \| css  | New `js/render/learning-paths.js` parses track markdown, cross-references completions state, renders per-track progress in new `## Learning Paths` section of `content/dsa/index.md`. Full-width "Mark as completed" button after `.article-end-marker`, one-way + undo-link. | Backlog | -          | Depends on WIKI-461 for completions data. Track sequence content (3 tracks) per `docs/superpowers/specs/2026-07-17-dsa-interview-prep-path-design.md`. | p2       | 8            |
| WIKI-429 | 2026-07-14 | Cross-site session cookie blocked by Safari ITP        | bug      | auth                      | Cross-origin session cookie (SameSite=None) is silently blocked/evicted by Safari ITP (confirmed) and other browsers' cross-site cookie restrictions; user logs in, then is immediately treated as logged out. | In Progress |         | Switching session auth from cookie to Authorization: Bearer token in localStorage; drops now-redundant CSRF double-submit mechanism. Paired with WIKI-BE-27.                        | p1       | 5            |
| WIKI-466 | 2026-07-25 | Cross-tab sync: settings/theme + login/logout don't propagate to other tabs | bug | storage \| auth | From UI-components audit, live-verified. (1) `window.addEventListener("storage",...)` in `settings-theme.js:801` never checks `SETTINGS_KEY` - a theme/font/background change in tab A never updates tab B's DOM despite the storage event firing with correct data (confirmed live with two real tabs). (2) `auth.js`'s cross-tab session-sync listener updates login/logout UI in other tabs but never calls `Sync.pullAll()` (login) or `Sync.clearUserDataCache()` (logout) - other tabs show "logged in"/"logged out" chrome while bookmarks/reads/recents in localStorage still reflect the previous identity. | Backlog | | Confirmed: `settings-theme.js` storage listener only branches on `BOOKMARKS_KEY`/`RECENTS_KEY`/read-key, no `SETTINGS_KEY` case. Fix (1): add `else if (e.key === SETTINGS_KEY) applySettingsToDOM(...)`. Fix (2): mirror `Auth.login`/`Auth.logout`'s own sync calls in the storage-listener branch. | p2 | 3 |
| WIKI-483 | 2026-08-03 | Graph overlays lack zoom, pan, and visible node labels | feature | app/graph-engine | Link Graph, Section Map, and Index Graph (all built on `js/app/graph-engine.js`) show unlabeled dots with no zoom or pan - labels only appear on hover, no wheel-zoom, no drag-to-pan, no touch pinch/pan. | Backlog | - | Needs proper discussion before implementation - open questions: always-on vs zoom-threshold labels, minimap, search-to-locate-node, touch gesture scope. | p3 | - |
| WIKI-488 | 2026-08-03 | Login/register input autofill styling clashes           | bug      | css/components-auth       | Browser-native yellow autofill background on email/password inputs in the auth modal clashes with the app's palette (`css/components-auth.css`). | Backlog | -          | Fix: add `input:-webkit-autofill { box-shadow: 0 0 0px 1000px <surface-color> inset; -webkit-text-fill-color: <text-color>; }` overrides scoped to auth modal inputs.                                          | p4       | 1            |
| WIKI-490 | 2026-08-03 | Article path format mismatch breaks isRead() lookups for the open article | bug | render \| storage | `state.currentFilePath` (set via `normalizePath()` in `content-view.js`, no `./` prefix) never matches index/search-derived `entry.path` values (all carry a `./` prefix, from `scripts/build_search_index.py`'s hardcoded `base_path = "./content/{wiki}"`). `isRead(state.currentFilePath)` always misses when checked against any bulk/index-derived path list. | Backlog | - | Found while testing WIKI-482: the currently-viewed article can never appear in a bulk read/unread command's changed set, so its read-toggle button can't be verified (or correctly updated) via that path today. Fix likely in `build_search_index.py` (drop the `./` prefix) or a shared path-normalization step before any `isRead()` comparison - needs root-cause decision, not a blind strip. | p2 | 3 |
| WIKI-520 | 2026-08-03 | Mark-read-by-section dot flashes visible then disappears | bug | search \| render/home-index | `test_mark_read_section_argument_marks_only_that_section` (CI flake investigation): `.index-card-read-dot.visible` passes `wait_for_selector` then immediately reads `count() == 0` on the same selector - dot appears then is wiped by a later re-render, not a load artifact (reproduced isolated from the parallel-CI resource issue). | Backlog | - | `_resolveSectionArg`'s `run()` in `js/search.js:132` captures `changed` in a closure at preview-build time, then calls `navigate(wiki.id)` after `markRead()`; suspect a second async index re-render (fresh `isRead()` read racing the localStorage write, or double navigate) overwrites the just-rendered `.visible` class. Needs systematic-debugging session - root cause not yet isolated to one call site. | p2 | 3 |
| WIKI-521 | 2026-08-03 | Install-prompt toasts vs other boot-time toasts ordering | bug | app/install-prompt \| render/toast | `app/install-prompt.js` queues an install/iOS-nudge toast (8000ms duration) via the shared `showToast` queue at page load; unverified whether it wins/blocks queue ordering against other early toasts (e.g. auth/sync) firing around the same boot window. | Backlog | - | Check `render/toast.js`'s FIFO queue against real boot-time toast sources; confirm whether install-prompt's long duration delays a more time-sensitive toast behind it. Raised while updating `ui-components-audit-agent-prompt.md`. | p4 | 2 |
