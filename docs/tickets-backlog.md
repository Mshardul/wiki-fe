# Wiki App - Task Board (Backlog)

| ID       | Entry Date | Summary (upto 7 words)                               | Type     | Component                 | Description (upto 30 words)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Status  | Impl. Date | Remarks (upto 30 words)                                                                                                                                                               | priority | story points |
| -------- | ---------- | ---------------------------------------------------- | -------- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------ |
| WIKI-046 | 2026-04-22 | Offline bulk download                                | feature  | sw \| settings            | List cached articles in settings. Add bulk download.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Backlog | -          | Requires path normalisation mapping to SW Cache keys.                                                                                                                                 | p2       | 8            |
| WIKI-299 | 2026-06-22 | Background sync - queue offline writes, flush on reconnect | feature  | sw \| storage             | Bookmark adds/removes and note saves made while offline are queued in localStorage and flushed to the backend when the connection is restored, using the Background Sync API where available. | Backlog | -          | storage.js: wrap backend write calls in a queue when `!navigator.onLine`; register a sync tag via SW on queue write. SW `sync` handler flushes the queue via `api.js`. Degrades silently on browsers without Background Sync. | p5       | 8            |
| WIKI-461 | 2026-07-18 | Completion-tracking primitive (storage + api)        | feature  | storage \| api            | New `js/storage/completions.js` mirroring `read-tracking.js`: localStorage `wiki-completed-{wikiId}` Set, `sequencedMutation` + new `api.completions.add/remove`. Generic per-wiki-per-article "mark as completed" state, distinct from read-state. | Backlog | -          | Depends on WIKI-BE-42 for logged-in DB sync; anon localStorage-only path ships independent of BE. | p2       | 5            |
| WIKI-462 | 2026-07-18 | DSA Learning Paths: index section + completion button | feature  | render \| content \| css  | New `js/render/learning-paths.js` parses track markdown, cross-references completions state, renders per-track progress in new `## Learning Paths` section of `content/dsa/index.md`. Full-width "Mark as completed" button after `.article-end-marker`, one-way + undo-link. | Backlog | -          | Depends on WIKI-461 for completions data. Track sequence content (3 tracks) per `docs/superpowers/specs/2026-07-17-dsa-interview-prep-path-design.md`. | p2       | 8            |
| WIKI-429 | 2026-07-14 | Cross-site session cookie blocked by Safari ITP        | bug      | auth                      | Cross-origin session cookie (SameSite=None) is silently blocked/evicted by Safari ITP (confirmed) and other browsers' cross-site cookie restrictions; user logs in, then is immediately treated as logged out. | In Progress |         | Switching session auth from cookie to Authorization: Bearer token in localStorage; drops now-redundant CSRF double-submit mechanism. Paired with WIKI-BE-27.                        | p1       | 5            |
| WIKI-466 | 2026-07-25 | Cross-tab sync: settings/theme + login/logout don't propagate to other tabs | bug | storage \| auth | From UI-components audit, live-verified. (1) `window.addEventListener("storage",...)` in `settings-theme.js:801` never checks `SETTINGS_KEY` - a theme/font/background change in tab A never updates tab B's DOM despite the storage event firing with correct data (confirmed live with two real tabs). (2) `auth.js`'s cross-tab session-sync listener updates login/logout UI in other tabs but never calls `Sync.pullAll()` (login) or `Sync.clearUserDataCache()` (logout) - other tabs show "logged in"/"logged out" chrome while bookmarks/reads/recents in localStorage still reflect the previous identity. | Backlog | | Confirmed: `settings-theme.js` storage listener only branches on `BOOKMARKS_KEY`/`RECENTS_KEY`/read-key, no `SETTINGS_KEY` case. Fix (1): add `else if (e.key === SETTINGS_KEY) applySettingsToDOM(...)`. Fix (2): mirror `Auth.login`/`Auth.logout`'s own sync calls in the storage-listener branch. | p2 | 3 |
| WIKI-467 | 2026-07-25 | Boot-window writes silently lost for logged-in users on fast reload | bug | auth \| storage | From UI-components audit. `Auth.init()` is called un-awaited at boot; `state.session.status` stays `"loading"` until `GET /auth/me` resolves. Any bookmark/read/recent write in that window is local-only (`_loggedIn()` false), then `Sync.pullAll()` - which runs right after session confirms - unconditionally overwrites localStorage with server truth, discarding the write with no error. | Backlog | | Real and likely for fast/returning users acting within the first render frame. Fix direction (pick one): block interactive writes until session status resolves; queue-and-replay writes made during "loading"; or make `pullAll()` merge instead of overwrite for keys touched after boot started. | p1 | 5 |
| WIKI-473 | 2026-07-25 | TOC collapse state split across two localStorage keys, desyncs on reload | bug | content \| storage | From UI-components audit. TOC sidebar chevron (`toc.js:42`, key `wiki-toc-h2-*`) and the in-content per-heading collapse button (`toc.js:170`, key `wiki-heading-collapsed-*`) persist the same visual section's collapse state to two different keys. Same-session mirroring (`_syncContentH2`) makes them look in sync, but a reload restores each control from its own key independently - whichever control wasn't used to collapse reverts to expanded. | Backlog | | Clean, isolated, confirmed bug. Fix: use one shared key format (recommend standardizing on `wiki-heading-collapsed-*`) for both the TOC chevron and the in-content button's read/write paths. | p2 | 2 |
| WIKI-482 | 2026-07-25 | Bulk read/unread commands don't refresh the read-toggle button | bug | render \| storage | From UI-components audit. Bulk mark-as-read/unread commands (search `/` commands) update `read-tracking.js`'s stored state but never call `updateReadBtn()`, so the topbar read-toggle button keeps showing the pre-bulk-action state until the next navigation re-renders it. | Backlog | | Isolated one-call fix - add the existing `updateReadBtn()` call at the end of the bulk command handler(s). | p3 | 1 |
| WIKI-496 | 2026-07-27 | Six exported symbols have zero external importers | refactor | render \| content \| storage | From codebase-quality audit, confirmed via project-wide export/import grep. `openSectionMap`, `isSidecarTocOpen`, `depth-fold.js`'s `MAX_DEPTH`/`MIN_DEPTH`, `router.js`'s `nearestSlugMatch`/`resolveSlugAndRender`, `changelog-view.js`'s `parseChangelog`, `settings-theme.js`'s `saveSettings` are all only used within their own declaring file. | Backlog | | Each still used internally (not dead code), just over-exposed. Fix: prefix with `_` and drop from each file's `export` list, or leave deliberately public per-symbol if a future caller is expected. | p4 | 1 |
| WIKI-508 | 2026-07-30 | Ligature font mangles operators in rendered code blocks | bug | css \| content | `--font-mono` (`tokens.css:47`) uses ligature fonts (JetBrains Mono/Fira Code/Cascadia Code); applied to `.markdown-body pre code` (`code.css:41-46`) with no `font-variant-ligatures: none`, so `->`/`!=` visually render as `→`/`≠` in fenced code blocks - confusing for beginners copying code, though `textContent`/copy-button is unaffected (source stays correct ASCII). | Backlog | - | Fix: add `font-variant-ligatures: none;` (+ `font-feature-settings: "liga" 0, "calt" 0;`) scoped to code-block selector only - keep ligatures for prose/headings, don't touch `--font-mono` var itself. | p2 | 1 |
| WIKI-509 | 2026-07-30 | Breadcrumb: current-page crumb over-truncates, middle crumb ellipsizes mid-word | bug | css | `.content-topbar .breadcrumb a`/`.breadcrumb-link`/`span:last-child` (`topbar.css:56-70`) all share the same `min-width: 2.5em` + `flex-shrink: 1` with no protection for the last (current-page) crumb - on longer titles it collapses to a single letter while a middle crumb ellipsizes mid-word (e.g. "Dat..." for "Data Structures"). | Backlog | - | Fix: give last crumb `flex-shrink: 0` / higher `min-width` so it never loses the shrink fight; if breadcrumb still overflows, collapse a middle crumb to a single `…` segment instead of per-character mid-word ellipsis. | p3 | 2 |
| WIKI-510 | 2026-07-30 | Overflow-menu topbar icons: missing tooltips + 4-way icon collision | bug | css \| content | (1) All 8 `.topbar-overflow-item` buttons (`index.html` ~L305-433) have `aria-label` but no `title`, unlike the trailing Search/Preferences/Login icons which have both - no hover tooltip for sighted mouse users. (2) Distraction-free, Switch wiki, Link graph, and Compare complexity (`index.html` L342/350/358/366) all reference the same `#icon-grid` symbol - visually indistinguishable in the topbar. | Backlog | - | Fix (1): add `title` attrs matching existing pattern. Fix (2): sprite (`sprite.svg`) is hand-maintained, only 27 of Tabler's 5,900+ icons pulled in - add 4 distinct Tabler outline icons as new `<symbol>` blocks and repoint the 4 `<use href>` refs. Icon names not yet chosen. | p3 | 2 |
