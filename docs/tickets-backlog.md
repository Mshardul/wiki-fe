# Wiki App - Task Board (Backlog)

| ID       | Entry Date | Summary (upto 7 words)                               | Type     | Component                 | Description (upto 30 words)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Status  | Impl. Date | Remarks (upto 30 words)                                                                                                                                                               | priority | story points |
| -------- | ---------- | ---------------------------------------------------- | -------- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------ |
| WIKI-046 | 2026-04-22 | Offline bulk download                                | feature  | sw \| settings            | List cached articles in settings. Add bulk download.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Backlog | -          | Requires path normalisation mapping to SW Cache keys.                                                                                                                                 | p2       | 8            |
| WIKI-299 | 2026-06-22 | Background sync - queue offline writes, flush on reconnect | feature  | sw \| storage             | Bookmark adds/removes and note saves made while offline are queued in localStorage and flushed to the backend when the connection is restored, using the Background Sync API where available. | Backlog | -          | storage.js: wrap backend write calls in a queue when `!navigator.onLine`; register a sync tag via SW on queue write. SW `sync` handler flushes the queue via `api.js`. Degrades silently on browsers without Background Sync. | p5       | 8            |
| WIKI-461 | 2026-07-18 | Completion-tracking primitive (storage + api)        | feature  | storage \| api            | New `js/storage/completions.js` mirroring `read-tracking.js`: localStorage `wiki-completed-{wikiId}` Set, `sequencedMutation` + new `api.completions.add/remove`. Generic per-wiki-per-article "mark as completed" state, distinct from read-state. | Backlog | -          | Depends on WIKI-BE-42 for logged-in DB sync; anon localStorage-only path ships independent of BE. | p2       | 5            |
| WIKI-462 | 2026-07-18 | DSA Learning Paths: index section + completion button | feature  | render \| content \| css  | New `js/render/learning-paths.js` parses track markdown, cross-references completions state, renders per-track progress in new `## Learning Paths` section of `content/dsa/index.md`. Full-width "Mark as completed" button after `.article-end-marker`, one-way + undo-link. | Backlog | -          | Depends on WIKI-461 for completions data. Track sequence content (3 tracks) per `docs/superpowers/specs/2026-07-17-dsa-interview-prep-path-design.md`. | p2       | 8            |
| WIKI-429 | 2026-07-14 | Cross-site session cookie blocked by Safari ITP        | bug      | auth                      | Cross-origin session cookie (SameSite=None) is silently blocked/evicted by Safari ITP (confirmed) and other browsers' cross-site cookie restrictions; user logs in, then is immediately treated as logged out. | In Progress |         | Switching session auth from cookie to Authorization: Bearer token in localStorage; drops now-redundant CSRF double-submit mechanism. Paired with WIKI-BE-27.                        | p1       | 5            |
| WIKI-461 | 2026-07-24 | Hover-preview shows raw prereq/TOC bullets, not summary | bug | render \| content | `showHoverPreview`'s markdown-scrape fallback (`content-view.js`) picks the first paragraph not literally prefixed "prerequisites"/"table of contents", but list items under those headings (e.g. `- **Big-O Notation** [Must read]...`) pass the filter untouched, so hovering a prereq chip shows a prereq bullet from the target page instead of a real summary. | Backlog | | Root cause confirmed live on `stack.md` hovering `array.md`/`linked-list.md` prereq chips - `SKIP_PREFIXES` only matches the heading line itself (already excluded by the `#` check), never the section body. Decided fix: stop scraping markdown entirely - add hand-authored `data/summaries.json` (flat map, path/slug -> summary string, same pattern as `data/glossary.json`); `showHoverPreview` reads from it directly. | p2 | 3 |
| WIKI-462 | 2026-07-24 | Glossary term popovers wired but zero content adoption | content | content | `addInlineGlossaryExpand`/`data/glossary.json` (click-to-expand term definitions) is fully implemented in `glossary-caveats.js` but no article in `content/*.md` uses an `<abbr>` tag, so the feature has never actually fired for a reader. | Backlog | | Confirmed via repo-wide search - zero `<abbr>` usage in `content/`. Needs a content pass: wrap glossary-matching terms (e.g. amortized, invariant, lru, big-o) in `<abbr>` across existing articles, starting with high-traffic DSA pages. | p3 | 3 |
| WIKI-463 | 2026-07-25 | Auth: verify-result heading + forgot/reset panel stale state | bug | auth | From FE audit (`auth-ux-audit - 20260713.md`). (1) Verify-result panel `<h2>` (`index.html:1114`) never updates off "Verifying your email" on any outcome - static markup, never referenced in `auth.js`. (2) `#auth-forgot-sent` success message not in `_clearErrors()`'s reset list - can show stale on a later, unsubmitted forgot-password attempt. (3) Register/reset panel swap re-renders checklist to all-red via empty string but never resets the actual input value or submit-disabled state - desyncs from real field content. | Backlog | | Code-confirmed against current `js/auth.js`/`index.html`. Audit's separate resend-email-goes-to-wrong-field finding was checked and found already fixed (`_pendingVerifyEmail` tracking exists) - not included here. Supersedes WIKI-477 (dropped as duplicate, see tickets-archive.md). | p2 | 5 |
| WIKI-464 | 2026-07-25 | Auth: anon-to-account migration can silently lose local data | bug | auth | From FE audit. `maybeMigrate()` (`js/auth.js:52-87`) swallows `api.importAll(payload)` failure with an empty catch, then unconditionally proceeds to `Sync.pullAll()`, which overwrites local storage with server truth - if import failed, the user's "Keep them" choice is silently discarded with no error and no recovery path. | Backlog | | Highest-severity finding in the auth audit - explicit user action ("Keep them") produces no error but results in data loss on the next line. Fix: don't swallow import error - show error toast and skip the subsequent `pullAll()` overwrite on failure. Supersedes WIKI-475 (dropped as duplicate, see tickets-archive.md). | p1 | 3 |
| WIKI-465 | 2026-07-25 | Auth: error messages leak raw network text / resend always claims success | bug | auth | From FE audit. (1) `NETWORK` `ApiError`s carry the raw browser fetch-failure string (e.g. "Failed to fetch") and every auth catch block shows `e.message` verbatim - violates the app's own "no raw technical text to user" rule. (2) `resend()` shows "Verification email sent" unconditionally, including on network failure/rate-limit/backend-down - only justified for anti-enumeration on auth-domain errors, not network errors. | Backlog | | Same root cause (undifferentiated error-code handling) across login/register/forgot/reset/resend - one ticket. Fix: branch on `e.code`, give `NETWORK` its own FE-authored message; keep uniform-success only for auth-domain codes on resend. | p2 | 3 |
| WIKI-466 | 2026-07-25 | Cross-tab sync: settings/theme + login/logout don't propagate to other tabs | bug | storage \| auth | From UI-components audit, live-verified. (1) `window.addEventListener("storage",...)` in `settings-theme.js:801` never checks `SETTINGS_KEY` - a theme/font/background change in tab A never updates tab B's DOM despite the storage event firing with correct data (confirmed live with two real tabs). (2) `auth.js`'s cross-tab session-sync listener updates login/logout UI in other tabs but never calls `Sync.pullAll()` (login) or `Sync.clearUserDataCache()` (logout) - other tabs show "logged in"/"logged out" chrome while bookmarks/reads/recents in localStorage still reflect the previous identity. | Backlog | | Confirmed: `settings-theme.js` storage listener only branches on `BOOKMARKS_KEY`/`RECENTS_KEY`/read-key, no `SETTINGS_KEY` case. Fix (1): add `else if (e.key === SETTINGS_KEY) applySettingsToDOM(...)`. Fix (2): mirror `Auth.login`/`Auth.logout`'s own sync calls in the storage-listener branch. | p2 | 3 |
| WIKI-467 | 2026-07-25 | Boot-window writes silently lost for logged-in users on fast reload | bug | auth \| storage | From UI-components audit. `Auth.init()` is called un-awaited at boot; `state.session.status` stays `"loading"` until `GET /auth/me` resolves. Any bookmark/read/recent write in that window is local-only (`_loggedIn()` false), then `Sync.pullAll()` - which runs right after session confirms - unconditionally overwrites localStorage with server truth, discarding the write with no error. | Backlog | | Real and likely for fast/returning users acting within the first render frame. Fix direction (pick one): block interactive writes until session status resolves; queue-and-replay writes made during "loading"; or make `pullAll()` merge instead of overwrite for keys touched after boot started. | p1 | 5 |
| WIKI-468 | 2026-07-25 | Stale-navigation races: mermaid render, related/backlinks/bridges panels, quiz-me poll | bug | render \| content | From UI-components audit. Three call sites lack the render-generation guard (`_renderGen`) that `content-view.js`'s own pipeline already uses: `renderMermaidDiagrams` (mermaid.js), `renderRelatedArticles`/`renderBacklinks`/`renderBridges` (related-articles.js, persistent DOM containers reused across navigations), and the `/quiz` search command's render-ready poll (search.js). Fast/slow navigation can paint one article's content into another's DOM. | Backlog | | Same fix pattern across all three - thread the existing `gen`/`_renderGen` token through and bail on mismatch, mirroring the guard already used for `fetchText` elsewhere in `content-view.js`. | p2 | 5 |
| WIKI-469 | 2026-07-25 | Modal stacking: no body scroll-lock, swipe-close/shortcut gaps, toast z-index | bug | ui \| css | From UI-components audit, live-verified. (1) No `body.modal-open`-style scroll lock exists anywhere - background content scrolls behind search/prefs/auth modals via wheel/keyboard (live-verified: `window.scrollY` moved under a modal). (2) Mobile swipe-down `closeTopPanel()` chain omits wiki-switcher and bookmarks modals, unlike the Escape handler which covers both. (3) Single-letter content shortcuts (b/f/h/i/t/d/q/w/g/+/-) fire through open modals when focus is on a non-input element inside them (live-verified: pressing `w` while bookmarks modal open stacks the wiki switcher on top). (4) `.wiki-toast` uses `--z-modal-backdrop` (1100) instead of the already-defined-but-unused `--z-toast` (9000) token - toasts fired while any modal is open render invisibly underneath it (live-verified). | Backlog | | All 4 confirmed against current CSS/JS. (4) is the cheapest fix - one CSS var swap. (1)-(3) share the "no shared modal-open registry" root cause. | p2 | 5 |
| WIKI-470 | 2026-07-25 | Distraction-free mode flag desyncs from DOM on navigation | bug | content \| render | From UI-components audit. `content-view.js`'s render teardown strips the `distraction-free` body class directly (line 269) instead of calling `exitDistractionFree()`, so `distraction-free.js`'s internal `_distractionFree` flag stays stale `true` after navigating away. Next article: Escape falsely triggers the "Reset view?" confirm dialog, and the toggle button's first click silently no-ops. | Backlog | | Isolated, well-understood fix - teardown should call `exitDistractionFree()` (imported from distraction-free.js), matching the pattern already used for `cleanupFocusMode()`/`cleanupStudyMode()`/`cleanupInterviewMode()` in the same teardown block. | p3 | 2 |
| WIKI-471 | 2026-07-25 | Mobile touch targets: TOC drawer items + topbar icons under 44px | bug | ui \| css | From mobile-UX audit. (1) `.toc-item`/`.toc-group-chevron`/`.toc-collapse` never get a `(pointer:coarse)` 44px bump in `responsive.css`, despite being the primary mobile-drawer navigation control - every comparable control elsewhere got this treatment. (2) `.content-topbar .topbar-icon-btn` at `max-width:375px` (34px, `responsive.css:344`) has higher CSS specificity than the `(pointer:coarse)` 44px fix (`responsive.css:430`), so the touch-target fix silently never applies at 360-375px viewports - a pure specificity bug, not a missing rule. | Backlog | | (2) is a confirmed cascade/specificity deduction (not yet pixel-verified live). Fix (1): add the three TOC selectors to the existing `(pointer:coarse)` block. Fix (2): raise the coarse-pointer rule's specificity to match/exceed the width-based one, or move the touch-target minimum into the `max-width:375px` block itself via padding rather than min-width/height. | p3 | 3 |
| WIKI-472 | 2026-07-25 | iOS input auto-zoom: 4 text inputs computed under 16px | bug | ui \| css | From mobile-UX audit, needs live iOS verification. App's own "S" font-size setting sets root `<html>` font-size directly (not a scale multiplier) - at "S" (14px root), `.auth-field input` and `.gsearch-input` (`--text-base`) drop under the 16px iOS Safari auto-zoom threshold. `.notes-scratchpad-input` (`--text-xs`) and `.article-find-input` (`--text-sm`) are under 16px at every font-size setting including "L". | Backlog | | Arithmetic from source token values, not live-measured - flag for a real iOS Safari check before/after fix. Fix: floor form-control font-size at an absolute 16px (e.g. `max(16px, var(--text-base))` or a dedicated `--text-input-min` token) independent of the user's scalable root font-size, applied once across all 4 inputs. | p3 | 2 |
| WIKI-473 | 2026-07-25 | TOC collapse state split across two localStorage keys, desyncs on reload | bug | content \| storage | From UI-components audit. TOC sidebar chevron (`toc.js:42`, key `wiki-toc-h2-*`) and the in-content per-heading collapse button (`toc.js:170`, key `wiki-heading-collapsed-*`) persist the same visual section's collapse state to two different keys. Same-session mirroring (`_syncContentH2`) makes them look in sync, but a reload restores each control from its own key independently - whichever control wasn't used to collapse reverts to expanded. | Backlog | | Clean, isolated, confirmed bug. Fix: use one shared key format (recommend standardizing on `wiki-heading-collapsed-*`) for both the TOC chevron and the in-content button's read/write paths. | p2 | 2 |
| WIKI-474 | 2026-07-25 | Router: rapid navigate() calls push history entry that never renders | bug | render | From UI-components audit. `navigate()` unconditionally calls `history.pushState` on every call, but `route()`'s internal `setTimeout(0)` debounce (`_routeTimer`) means only the last of two rapid back-to-back `navigate()` calls in one tick actually runs `_execRoute` - the first call's history entry exists but its target was never rendered, until a later Back press hits it via `popstate`. | Backlog | | Requires genuinely rapid double-navigation to trigger (double-click on two different nav targets, or two commands firing in one handler tick). Fix direction: drop the `setTimeout(0)` in `route()` and call `_execRoute` synchronously (since `navigate()` already computes the hash synchronously), or move `pushState` inside `_execRoute` so it only fires for the call that actually wins. | p3 | 3 |
| WIKI-476 | 2026-07-25 | Register/reset password checklist and submit-button desync on panel swap | bug | auth | From auth-UX audit. `AuthModal._swap()` (`auth.js:148-149`) force-renders the password checklist to all-red via `_renderChecklist(id, "")` on entering register/reset panels, but never touches the input's actual `.value` or the submit button's `disabled` state (last set by the `input` listener). Leaving and returning to the panel with a previously-valid password shows a false all-red checklist while the real input content and submit-enabled state are unchanged and may disagree with what's displayed. | Backlog | | Same bug pattern on both `auth-pw-checklist`/`auth-reg-submit` (register) and `auth-reset-pw-checklist`/`auth-reset-submit` (reset). Fix: re-derive checklist + submit-disabled from the input's current `.value` on swap (reuse the `input` listener's logic) instead of hardcoding an empty string. | p2 | 2 |
| WIKI-478 | 2026-07-25 | Network/timeout errors leak raw browser error text to auth UI | bug | auth \| api | From auth-UX audit. `api.js`'s `_request` throws `ApiError("NETWORK", networkErr.message, 0)` using the raw browser fetch-failure string (e.g. "Failed to fetch"). Every `auth.js` catch block does `e instanceof ApiError ? e.message : "..."`, so a dropped connection shows this raw technical string verbatim in the login/register/forgot/reset error `<p>`, violating the project's no-raw-technical-text-to-user rule. | Backlog | | `TIMEOUT` already has a proper FE-authored message ("Request timed out...") - only `NETWORK` needs its own branch. Fix: in each auth.js catch, check `e.code === "NETWORK"` first and show an FE-authored message ("Couldn't reach the server. Check your connection and try again.") before falling through to `e.message`. | p2 | 2 |
| WIKI-479 | 2026-07-25 | Modal focus-trap and open-state checks reimplemented per-modal, out of sync | refactor | ui | From UI-components audit. Focus-trap logic is separately reimplemented in 4 places (auth modal, preferences, search, and others) with no shared helper, and `isXOpen()`-style open-state checks are similarly duplicated per modal - wiki-switcher is missing one entirely and gets bypassed in `app.js` via a direct DOM check instead. No shared modal registry exists to unify either concern. | Backlog | | Root cause shared with WIKI-469 (modal stacking) but scoped separately since that ticket is about runtime bugs (scroll-lock/z-index/shortcuts) while this is the underlying duplication/refactor. Fix direction: extract one shared focus-trap helper and one shared modal-registry module (open/close/isOpen) that all modals route through, including wiki-switcher. | p3 | 5 |
| WIKI-480 | 2026-07-25 | Bookmarks modal z-index collision and reentrancy leak | bug | ui | From UI-components audit. (1) `bookmarks-modal.css:7` and `wiki-switcher.css:75` both hardcode `--z-wiki-switcher` for their own dialog's z-index, so bookmarks and wiki-switcher can render at the same stacking level. (2) `openBookmarksModal()` has no reentrancy guard - calling it while already open re-adds its focus-trap keydown listener each time, leaking listeners. | Backlog | | Two small, unrelated-cause bugs in the same file, batched together for one fix pass. Fix (1): give bookmarks-modal its own dedicated z-index token. Fix (2): no-op or re-close-then-reopen if already open, matching the guard pattern used elsewhere (e.g. `_withSubmitGuard`). | p3 | 2 |
| WIKI-481 | 2026-07-25 | Content enhancer failures cascade and skip bookmark/read/offline button sync | bug | render \| content | From UI-components audit. `content-view.js`'s stub-article render branch skips the bookmark/read/offline toolbar-button sync that the full-article branch runs, leaving those buttons showing stale state on stub pages. Separately, all ~30 content enhancers run inside one shared try/catch, so one enhancer throwing stops every enhancer after it in the block, including the button-sync calls if they're late in the sequence. | Backlog | | Two distinct causes, same symptom (toolbar buttons wrong/stale) - bundled as one ticket since the fix likely touches the same function. Fix: run the button-sync calls unconditionally (not gated on the stub/full branch), and wrap each enhancer individually rather than sharing one try/catch. | p2 | 3 |
| WIKI-482 | 2026-07-25 | Bulk read/unread commands don't refresh the read-toggle button | bug | render \| storage | From UI-components audit. Bulk mark-as-read/unread commands (search `/` commands) update `read-tracking.js`'s stored state but never call `updateReadBtn()`, so the topbar read-toggle button keeps showing the pre-bulk-action state until the next navigation re-renders it. | Backlog | | Isolated one-call fix - add the existing `updateReadBtn()` call at the end of the bulk command handler(s). | p3 | 1 |
| WIKI-483 | 2026-07-25 | Graph node-map construction triplicated across index-graph/link-graph/section-map | refactor | render \| nav | From codebase-quality audit. Index-graph, link-graph, and section-map overlays each independently build their own node-map/graph-data structure from the same backlinks/search-index source data, with duplicated path-normalization and node-click handling logic across all three instead of one shared builder. | Backlog | | Confirmed via full read of all three modules - same construction pattern copy-pasted 3x. Fix direction: extract one shared `buildGraphNodes()`-style helper (candidate home: `graph-engine.js` or a new shared module) that all three overlays call, covering node construction, path normalization, and click-handler wiring. | p3 | 5 |
| WIKI-484 | 2026-07-25 | Emoji markers corrupt code-block syntax highlighting | bug | content | `_createMarker`/`_insertMarkerBadge` (`js/content/highlights.js`) insert the emoji badge at the raw text-node offset with no check for a `<pre>`/`<code>` ancestor, so a marker dropped inside a code block splits the highlighted code's text node and visually reads as part of the snippet, which can look like a syntax error. | Backlog | | Fix direction: in `_showToolbar`'s selection handler (or `_createMarker` itself), detect `range.commonAncestorContainer.closest("pre, code")` and either hide the emoji buttons in the toolbar or no-op marker creation for that selection - highlight-only should still work inside code blocks. | p2 | 2 |
| WIKI-485 | 2026-07-25 | Emoji marker inline placement disrupts reading flow - needs redesign | design | content \| css | Emoji markers (`.wiki-marker`, `js/content/highlights.js`) render as a full-size glyph spliced directly into the text node at the click offset, which reads as visually disruptive mid-sentence. Needs a design pass exploring alternatives (margin gutter, hover-reveal, etc.) that keep the marker anchored at the exact point without breaking text flow. | Backlog | | Discussed several placement mockups (gutter flag, superscript dot, underline, chip list) - none landed, user wants to revisit later with fresh direction. Scope: prose text only, code blocks handled separately by WIKI-484. | p3 | 3 |
| WIKI-486 | 2026-07-27 | Register/reset forms: no confirm-password, no loading feedback | bug | auth | From auth-UX audit, code-confirmed. `index.html` register/reset panels each have one masked password input with no confirm field and no reveal toggle. `_withSubmitGuard` dims the submit button but never changes its label or disables the email/password inputs during the in-flight request (up to 15s timeout with zero other feedback). | Backlog | | Two related UX gaps on the same forms, bundled. Fix: add a show/hide toggle button (cheaper than confirm-password) and a "Creating…"/spinner submit state + input-lock during request. | p3 | 3 |
| WIKI-487 | 2026-07-27 | Verify-result panel: no resend action, no error-code branching | bug | auth | From auth-UX audit, code-confirmed. `verifyFromLink`'s catch block (`js/auth.js:319-333`) shows either the backend's raw message or one generic fallback for every failure type (expired/malformed/already-used token) with no branch on `e.code`, and the panel's only action is "Back to log in" - no way to request a fresh link without a login-fail detour first. | Backlog | | Fix: switch on `e.code` where the backend distinguishes cases, and add a "Resend verification email" action directly on this panel. | p3 | 3 |
| WIKI-488 | 2026-07-27 | Resend-verification button has no client-side cooldown | bug | auth | From auth-UX audit, code-confirmed. `#auth-resend-btn` (`js/auth.js:443-445`) is guarded against true double-clicks via `_withSubmitGuard` but nothing prevents repeated clicks seconds apart; combined with `resend()` always showing "sent" regardless of outcome, a user can hammer the button with no throttle signal. | Backlog | | Fix: disable + show a countdown ("Resend in 30s") after a successful send, independent of backend-side rate limiting. | p4 | 2 |
| WIKI-489 | 2026-07-27 | Auth forms: no aria-invalid on errored inputs, checklist state not screen-reader exposed | bug | auth | From auth-UX audit, code-confirmed. `AuthModal._showError` (`js/auth.js:184`) never sets `aria-invalid` on the related input. Separately, the password checklist's pass/fail state (`css/components-auth.css:89-97`) is conveyed only via a `::before` CSS glyph (✗/✓) - generated content that screen readers don't reliably announce - while the `<li>` text itself never changes. | Backlog | | Two related a11y gaps in the same auth-form surface, bundled. Fix: set/clear `aria-invalid` in `_showError`/`_clearErrors`; add visually-hidden "— met"/"— not met" text per checklist `<li>`. | p3 | 3 |
| WIKI-490 | 2026-07-27 | Auth copy/tone inconsistencies across login/register/forgot/reset | content | auth | From auth-UX audit, code-confirmed. "Sign up" appears in body copy while every button/title says "Log in"/"Create account"; `login()`/`register()`'s JS-exception fallbacks ("Login failed"/"Registration failed") are curter than sibling messages; "Could not send reset link." breaks the contraction/article pattern used everywhere else on the same panel. | Backlog | | Pure copy pass, no logic change. Fix: align prose to existing button language, warm up the two flat fallbacks, fix the one non-contracted string. | p4 | 1 |
| WIKI-491 | 2026-07-27 | Anon-migration "discard" is an implicit toast-timeout, not an explicit user choice | bug | auth | From auth-UX audit. `maybeMigrate()` (`js/auth.js:52-87`)'s only affirmative action is "Keep them" - not clicking it (dismissed, ignored, or the ~12.5s timer elapsing) is treated identically as "Discard" and clears the local data cache, with no way to distinguish an active choice from a missed toast. | Backlog | | Compounds risk given WIKI-464/475's silent-failure finding on the "Keep them" path itself. Fix direction: a longer-lived/non-expiring prompt or persistent banner for this specific one-time, data-loss-risking decision instead of a transient toast. | p3 | 2 |
| WIKI-492 | 2026-07-27 | Mobile: search-results overscroll, data-clear checkbox, migrate-modal, recent-chips touch gaps | bug | ui \| css | From mobile-UX audit, code-confirmed. (1) `.gsearch-results` lacks `overscroll-behavior:contain`, unlike the other two full-screen mobile modals. (2) `.data-clear-checkbox` (prefs Advanced tab) stays a fixed 16x16px with no `(pointer:coarse)` bump anywhere. (3) `.migrate-modal`/`.migrate-dialog` (anon→login prompt) has zero mobile-specific layout, unlike every other modal in the app. (4) `.gsearch-recent-chip`/`-remove` only get 44px at `max-width:640px`, missing the `(pointer:coarse)` tier for 641-900px tablets. | Backlog | | Four small, isolated CSS-only gaps in the same touch-target category as WIKI-471, bundled into one pass. Fix each per its existing sibling pattern in `responsive.css`. | p3 | 3 |
| WIKI-493 | 2026-07-27 | `console.*` calls in committed code violate the no-console rule | bug | state \| content \| render | From codebase-quality audit, code-confirmed. Three unconditional `console.warn` call sites - `js/state.js:31` (duplicate WIKIS id), `js/content/mermaid.js:21,341` (render failure), `js/render/home-index.js:904` (malformed index row) - violate CLAUDE.md/CONVENTIONS.md's explicit "no console.* in committed code" rule with zero user-visible signal in production. | Backlog | | `mermaid.js` already has a user-visible error-state fallback next to its console line - pure removal there. `home-index.js`/`state.js` are build/content-authoring errors; drop or route through the existing `?debug` overlay instead. | p3 | 2 |
| WIKI-494 | 2026-07-27 | `recents.js` write skips the sequencedMutation race-guard used by bookmarks/reads | bug | storage | From codebase-quality audit, code-confirmed. `addToRecents` (`js/storage/recents.js:22-28`) calls `api.recents.add` directly and unguarded, unlike `bookmarks.js`/`read-tracking.js`'s identical write pattern, both wrapped in `sequencedMutation`. Fires on every article navigation, including rapid back-to-back navigation - exactly the out-of-order double-fire scenario the helper exists to prevent. | Backlog | | Fix: wrap the call in `sequencedMutation(`${wikiId}\|${path}`, () => api.recents.add(...))` to match the other two modules. | p3 | 2 |
| WIKI-495 | 2026-07-27 | CLAUDE.md FILE MAP missing ~21 of 52 JS files | chore | docs | From codebase-quality audit, confirmed via `find js -name '*.js'` diff. Entire feature areas (admin view, all 3 graph overlays, interview mode, highlights/markers, notes, complexity-compare, toc-companion) have no FILE MAP entry, undermining the project's own explicit "route via subtables, don't read every file" convention for an AI agent working in this repo. | Backlog | | Fix: regenerate FILE MAP subtables against current `js/` tree; consider a periodic diff check so this doesn't silently drift again. | p3 | 3 |
| WIKI-496 | 2026-07-27 | Six exported symbols have zero external importers | refactor | render \| content \| storage | From codebase-quality audit, confirmed via project-wide export/import grep. `openSectionMap`, `isSidecarTocOpen`, `depth-fold.js`'s `MAX_DEPTH`/`MIN_DEPTH`, `router.js`'s `nearestSlugMatch`/`resolveSlugAndRender`, `changelog-view.js`'s `parseChangelog`, `settings-theme.js`'s `saveSettings` are all only used within their own declaring file. | Backlog | | Each still used internally (not dead code), just over-exposed. Fix: prefix with `_` and drop from each file's `export` list, or leave deliberately public per-symbol if a future caller is expected. | p4 | 1 |
| WIKI-497 | 2026-07-27 | Offline-download button stuck in "loading" state forever on fetch/cache failure | bug | storage | From UI-components audit, code-confirmed. `downloadArticle` (`js/storage/offline.js:35-43`) has no try/catch around `fetch`; a network failure/404 rejects unhandled, so `Offline.toggle()`'s `btn?.classList.remove("loading")` (line 119) never runs and the button is stuck non-functional until the user navigates away and back. Also an unhandled promise rejection. | Backlog | | Fix: wrap the fetch/cache-put sequence in try/catch inside `downloadArticle`, and/or wrap `Offline.toggle`'s await in try/finally so the loading class always clears and a toast surfaces the failure. | p2 | 3 |
| WIKI-498 | 2026-07-27 | Session-change event forces a full content re-render, tearing down active reading state | bug | render \| auth | From UI-components audit, code-confirmed. `wiki:session-changed`'s listener (`js/app.js:116-118`) unconditionally calls `route(location.hash.slice(1))` on any login/logout anywhere (including a cross-tab event), and `renderContent` has no same-path short-circuit - it always tears down focus/study/interview mode and rebuilds the article from a fresh fetch, even when the article itself didn't change. | Backlog | | Distinct from WIKI-466/467 (those are about sync not happening; this is the opposite - an over-aggressive re-render once a session event does fire). Fix: skip the re-route when `state.currentView === "content"` and the path is unchanged; only refresh session-dependent chrome (auth button, admin nav, toolbar sync states). | p2 | 3 |
| WIKI-499 | 2026-07-27 | Wiki index view inherits previous view's scroll position on navigation | bug | render | From UI-components audit, code-confirmed. `router.js:45` explicitly excludes `view-index` from the eager scroll-to-top reset (to avoid fighting its own restore), but `home-index.js`'s scroll-restore is deferred behind `requestIdleCallback`/`populateIndexReadTimes().finally()` with no synchronous top-of-page fallback - a wiki with no saved scroll position (first visit, or a large/slow-loading index) visibly sits at the previous view's scroll offset until the deferred chain completes. | Backlog | | Fix: scroll to 0 synchronously (or via a cheap microtask) at the top of `renderIndex` when there is no saved scroll position for this wiki. | p2 | 2 |
| WIKI-500 | 2026-07-27 | Search-index retry duplicates every entry into the shared cache | bug | search | From UI-components audit, code-confirmed. `retryGlobalSearch`/`loadAllSearchEntries` (`js/search.js:428-472`) re-populate `allSearchCache.entries` via `.push()` on every call but never clear the array first (`js/state.js:137`) - any retry (or a flaky first load that partially succeeds) leaves duplicate result rows for the rest of the session, and inflates `gSearchCount`. | Backlog | | Fix: reset `allSearchCache.entries = []` in `retryGlobalSearch` (or at the top of `loadAllSearchEntries`) alongside the existing `loaded`/`loading` flag resets. | p2 | 2 |
| WIKI-501 | 2026-07-27 | Glossary hover-popover IntersectionObserver leaks on every navigation | bug | content | From UI-components audit, live-verified by the audit (monkey-patched `IntersectionObserver`, observed +2 constructions per navigation with no plateau across 3 sequential article visits). `addGlossaryTerms` (`js/content/glossary-caveats.js:224`) creates a new observer per call with no stored reference and no disconnect anywhere in the codebase - unbounded growth over a long session. | Backlog | | Fix: store the observer on `state` (mirroring `state.tocObserver`) and disconnect the previous one before creating a new one, or disconnect in a content-view teardown hook. Fold in the related lazy-pruned glossary-expand Set (`resetGlossaryExpandTracking()` should be called explicitly on teardown, not left to incidental clicks). | p2 | 3 |
| WIKI-502 | 2026-07-27 | Pull-to-refresh on index view doesn't invalidate the global search cache | bug | render \| search | From UI-components audit, code-confirmed. `refreshIndex` (`js/render/home-index.js:708-714`) clears `indexCache` and its sessionStorage entry but never touches `allSearchCache` in `search.js` - a separate, independently-populated module cache - so ⌘K search keeps serving pre-refresh stale entries for that wiki until a full page reload. | Backlog | | Fix: clear/repopulate the relevant slice of `allSearchCache.entries` for the refreshed wiki inside `refreshIndex`, or expose a shared cache-invalidation hook both modules call into. | p3 | 2 |
| WIKI-503 | 2026-07-27 | Mermaid step-through and zoom-overlay both desync from a theme-change re-render | bug | content | From UI-components audit, code-confirmed. (1) `rerenderMermaidDiagrams` (`js/content/mermaid.js:315-348`) re-appends the existing step rail DOM (still showing e.g. "Step 3/5") onto the freshly-rendered SVG but never re-calls `_applyMermaidStep`, so node highlighting silently vanishes until the next Next/Prev click. (2) The zoom overlay holds a cloned, detached SVG (`zoom-lightbox.js:217-227`) that a theme change never touches, so an already-open zoom shows stale old-theme colors. | Backlog | | Same root cause (theme-change re-render not fully propagating to secondary UI), same file pairing, bundled. Fix (1): re-derive `stepIndex` from the existing rail and re-apply. Fix (2): re-clone the fresh body SVG into the overlay on theme change, or simply close the overlay. | p3 | 3 |
| WIKI-504 | 2026-07-27 | Table sort comparator is non-transitive for mixed numeric/non-numeric cells | bug | content | From UI-components audit, code-confirmed. `addTableSort`'s `doSort` (`js/content/tables.js:121-131`) only takes the numeric fast path when both compared cells parse as numbers; any pairwise comparison involving one numeric and one non-numeric cell (e.g. "N/A", "-") falls back to `localeCompare`, producing an inconsistent/non-transitive order across 3+ rows. | Backlog | | Common in complexity tables using "N/A"/footnote markers. Fix: pick one consistent rule for non-numeric cells (e.g. always sort after all numeric cells) instead of falling back to raw string comparison only for mixed pairs. | p3 | 2 |
| WIKI-505 | 2026-07-27 | Small isolated correctness gaps: LaTeX macro regex, backup-import validation, changelog parsing, pinch-zoom stale coords | bug | content \| storage | From UI-components audit, code-confirmed, four unrelated low-severity isolation bugs bundled for one fix pass. (1) `_substituteLatex` (`formatting.js:181-185`) can mis-tokenize a multi-letter macro immediately followed by a VAR_MAP single-letter variable with no separator. (2) `_validateBackup` (`settings-theme.js:341-347`) never validates the shape of dynamic `wiki-read-*` backup keys before writing them to localStorage. (3) `parseChangelog` (`changelog-view.js:16-39`) silently drops any entry with no backtick-wrapped filename instead of rendering it as plain text. (4) `zoom-lightbox.js`'s 2-finger pinch branch never resets `startX`/`startY` left over from an earlier 1-finger gesture, which can spuriously trigger swipe-dismiss on pinch-release. | Backlog | | Four independent low-traffic edge cases, bundled purely to avoid four single-line tickets. Fix each per its own direction noted in the source audit; no shared code path. | p4 | 3 |
