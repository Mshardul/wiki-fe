# DSA Article Writer

The **source of truth** for writing a DSA article (`content/dsa/**/*.md`). Given a topic, this file tells you what to write, in what shape, and in what order.

**Purpose: make the reader interview- AND competitive-programming-ready** - the theory, the working, and the reasoning, plus the CP toolkit. The two goals shape every param: not "is the article complete?" but "could a candidate walk into an interview _and_ sit a contest with only this page?" Passing [dsa-rater.md](./dsa-rater.md) is the _check_ that you hit that bar - not the goal itself. (See the wiki [readme](../../../readme.md#goals) for the per-vertical goal statement.)

This file **owns the rules**. The rater scores against the param IDs defined here (U1, DS2, PA1, FB, …); it does not redefine them. If a rule changes, change it here.

Companion files:

- [dsa-rater.md](./dsa-rater.md) - scores a finished draft against these params and gates ship / no-ship.
- [scripts/dsa-check.sh](../../../scripts/dsa-check.sh) - deterministic check for U8 / U11 / U12.
- **Category files** - this file covers everything shared across every specific article (universal params, format conventions, depth bar). Each category's own section block, family blocks, and headings list live in a dedicated file - read **only the one matching your target folder**, never all three:
  - [dsa-writer-ds.md](./dsa-writer-ds.md) - `data-structures/`
  - [dsa-writer-algorithm.md](./dsa-writer-algorithm.md) - `algorithms/`
  - [dsa-writer-pattern.md](./dsa-writer-pattern.md) - `patterns/`

---

## Article kinds - specific vs consolidated

Before anything else, decide which **kind** of article you're writing. The kind decides whether the structure below is mandatory.

- **Specific article** - covers **one** structure, algorithm, or pattern (`array`, `binary-search`, `sliding-window`). This is the default and the overwhelming majority. A specific article **must** follow its section's full structure: every universal param, the section block, the one family block, the ordered headings. The rater scores it against all of them and gates on the structure being present. Everything else in this file is written for specific articles.

- **Consolidated (hub) article** - covers a **family or cluster** of related topics that each deserve their own specific page, and exists to _survey and route_, not to teach any one of them in depth (`sorting` surveys merge/quick/heap/counting/radix; a future `tree-traversals` hub might survey pre/in/post/level-order). A hub is a **decision layer**: what the family is, the shared theory, a comparison table, "which one when", and a **linked list of its members** with a 2–3 sentence description each. It **does not** trace a single algorithm, and is therefore **exempt from the per-section structure** - no worked example, no single invariant, no one implementation, no family block, no practice problems are required, because those live on the member pages.

    **Mark a hub explicitly.** The first thing under the title (after Prerequisites/TOC) must be a blockquote declaring it: `> **Hub article.** This page is the survey + decision layer for <family> - it does not trace any single algorithm. Each <member> has its own page …`. This is the signal the rater keys on; an unmarked article is treated as specific and scored against the full structure.

    **A hub still owns:** U7/U8/U9/U10/U11/U12/U13 (format spine, title↔filename, prerequisites, TOC, soundbite, working links), U1 (what the family is + mental model), a member list with one working link per member (or plain-text name until the member page exists - never a broken `.md` link), and any genuinely family-level content (the comparison table, the shared lower-bound/theory, the "which one when" prose). It is judged on _those_, not on the per-algorithm params.

    **When to split into a hub + members vs. one specific article:** if a single page would have to trace **two or more genuinely different procedures** to be complete (sorting must trace merge _and_ quick _and_ counting), it's a family → hub + member pages. If one procedure with variants covers it (binary search + its boundary/exponential variants), it's one specific article. When unsure, default to specific.

- **Cheatsheet / meta article** - a lookup table or decision tool that aggregates across many specific articles (`pattern-selection-cheatsheet`, a future `complexity-cheatsheet`). It is **not** a specific article (no single topic), **not** a hub (no member pages to route to), and **not** scored against DS/AL/PA section params. It exists to serve a different reading mode: fast lookup during a contest or interview, not deep learning.

    **Mark a cheatsheet explicitly.** The first thing under the title (after Prerequisites/TOC) must be: `> **Cheatsheet.** This page is a fast-lookup aggregator - it does not teach any topic in depth. Each entry links to its full article.`

    **A cheatsheet owns:** U7/U8/U9/U10/U11/U12 (format spine, filename, TOC, working links) and its core lookup table(s). It is exempt from U1 mental model, U5 pseudocode, U6/U22/U23/U24/U25 practice problems, all section blocks, and all family blocks - those live on the specific article pages it links to.

    **Format rules for the lookup table:** rows = trigger condition (constraint, keyword phrase, or problem shape); columns = pattern/algorithm to reach for + why + link to full article. Every row must have a cross-link. The table must be scannable in under 5 seconds - no prose paragraphs inside cells.

    **`pattern-selection-cheatsheet.md` is the canonical example.** Its structure: one master table with columns `Trigger / constraint | Pattern | Why | Article`. Subsections by constraint size (`n ≤ 20`, `n ≤ 10⁵`, etc.) or by keyword cluster ("contiguous subarray", "all subsets", "shortest path"). Each subsection is a mini-table, not prose. A writer filling this must aggregate the `## Constraints & approach` tables from every pattern article and the `## Constraints & approach` tables from algorithm articles into one navigable surface.

    **Rater treatment:** run U7/U8/U9/U10/U11/U12 only. All other params are n/a with justification "cheatsheet article". Gate = SHIP if those six pass and the lookup table has ≥1 working cross-link per row.

---

## How to write one

1. **Pick section + family, then open only that category file.** Section from the target folder; family from the tables in that file (DS / Algorithm only - Patterns have none).
   - `content/dsa/data-structures/…` → **DS** → also read [dsa-writer-ds.md](./dsa-writer-ds.md)
   - `content/dsa/algorithms/…` → **Algorithm** → also read [dsa-writer-algorithm.md](./dsa-writer-algorithm.md)
   - `content/dsa/patterns/…` → **Pattern** → also read [dsa-writer-pattern.md](./dsa-writer-pattern.md)
2. **Create the file.** Create a new `.md` file at the target path (lowercase, hyphen-separated slug). No skeleton to copy - write from scratch using the **Headings list** in your category file as the structural spine, and fill every param in that order.
3. **Fill every section.** Apply three tiers of params: universal (every article) + the section block + the one family block. Each param below says exactly what "present at interview depth" means. **Advisory params must still be written** - they don't gate publish but they still belong in the article. Write them. The rater scores them; missing advisory content lowers the /100 score and shows up in FIXES.
4. **Write, then rate independently - never self-rate.** The agent/process that wrote the draft must not also grade it - that context is invested in its own choices and reads its own work generously. Once the draft exists, a **separate process with no memory of why any decision in the draft was made** reads [dsa-rater.md](./dsa-rater.md) and rates the file cold, the way an independent reviewer would. In an agentic session, this means spawning a fresh Agent for the rating pass rather than rating in the same context that wrote it (see `.prompts/fe-write-content.md` in the root wiki repo for the reference flow). Fix every **blocker** (gated param scoring ≤5), then re-rate with **another fresh, independent process** - not the same one that produced the prior verdict, and not the writer that just fixed it. Do not hand off a draft that reads NO-SHIP. Iterate write-fix → independent-rate until SHIP.
5. **Run the filesystem check** before declaring done: `../../../scripts/dsa-check.sh <article.md>` - fix any U8/U11/U12 FAIL.
6. **Register in the index - first draft only.** A new article is **invisible to the app until it's listed** in `content/dsa/index.md` (the app loads only files reachable from there - nothing globs the directory). So, the **first time** an article passes the checks above, add one row to the matching table in `index.md` (`## Data Structures` / `## Algorithms` / `## Patterns`): `| [Title](./<folder>/<slug>.md) | one-line description |`. **This step runs once, at first publish only** - on every later revision of an existing article, **skip it** (the row already exists; don't add a duplicate, and don't touch the row unless the title or one-liner genuinely changed). Quick check: is this slug already in `index.md`? If yes → revision, skip. If no → first draft, add the row.

---

## Format conventions (every article)

- Open `# Title` → `## Prerequisites` → `## Table of Contents` → body. **No YAML front matter** - the file must not start with `---` (the search index reads headings).
- Filenames: lowercase, hyphen-separated, `.md`.
- H1 title must slugify to the filename (case-insensitive): `# Hash Table` → `hash-table.md`.
- Prerequisites: `[Title](./path.md) [Must read]` or `[Title](./path.md) [Should read]` - name and tier only (tier is one of these two values, see U9), no trailing reason text (the rendered chip shows name + tier; a reason sentence never reaches the reader, so don't write one). For a not-yet-written target, use an HTML-comment link so it doesn't break: `<!-- [Title](./path.md) [Must read] -->`. **This is the one canonical format for all pending links** - in prerequisites, in cross-links inside body text, in hub member lists, in DS5 variant entries that point forward. Never write a plain `.md` link to a file that doesn't exist; never use ad-hoc "coming soon" prose.
- Cross-vertical links allowed and encouraged (e.g. DSA `hash-table` → `../../system-design/components/consistent-hashing.md`). Every live `.md` link must resolve to a real file.
- **Diagrams are real, never placeholders.** Use rendered mermaid or ASCII art - Showdown+Mermaid is configured. A `<!-- diagram -->` TODO does not count. **The diagram must match the prose walkthrough exactly** - same node/step count, same values, same order. If the worked example traces `[3, 1, 4, 1, 5]`, the diagram traces the same array, not a different illustrative one.
- **Pseudocode ≠ Python.** They have different jobs and must look different on the page. Pseudocode = the contract (CLRS-style: procedure name as a header line with no line numbers, `for i = 1 to n`, `▷ comment`, explicit `swap A[i] A[j]`, explicit indices, indentation for structure). Python = the reference (idiomatic: type hints, comprehensions, `enumerate`, `collections.deque`, `heapq`, unpacking). **Test: if the pseudocode could be pasted as valid Python, it failed.**
- **Dual-file scenario (algorithm + pattern overlap).** Some topics have both an algorithm article (`/algorithms/backtracking.md`) and a pattern article (`/patterns/backtracking.md`). These serve different purposes and must not repeat each other: the **algorithm article** covers the procedure, proof, correctness, worked example, and complexity - it teaches _how_ it works. The **pattern article** covers recognition signals, the reusable skeleton, and worked problems showing the skeleton applied - it teaches _when_ to reach for it and how to transfer it. The algorithm article must cross-link to the pattern article under `## When to use / when not`, and the pattern article must cross-link to the algorithm article under `## Related`. Both links must resolve (live `.md` links, not comments).

---

## Depth bar - write for a senior, not a beginner

Every param below states what must be _present_. This section states how _deep_ it must go. Presence is necessary, not sufficient.

**Write to the level a staff/senior interviewer would probe to.** A correct-but-shallow answer (the kind a strong junior gives) is **not** a full-credit article. Concretely, the high end of each param means:

- **Go past the obvious.** State the non-obvious cost, not just the headline O(): amortized vs worst-case, the constant factor that bites in practice, the cache effect, the resize/rehash hidden behind "O(1)". If a fact is what _separates_ a senior answer from a junior one, it belongs in the article.
- **Name the trade, not just the choice.** "Use X" is junior. "Use X over Y because A, accepting cost B" is senior. Every recommendation carries its trade-off.
- **At least one trap per article that a junior misses and a senior hits** - overflow on accumulation, iterator invalidation, the 1/4-shrink-not-1/2, the off-by-one in binary-search bounds. The Gotchas section is where seniority shows.

The rater's 9–10 band is reserved for articles that clear this bar. Present-but-shallow caps at 6–8.

### Build, don't repeat (section overlap)

Adjacent sections must **layer**, not restate. The two classic overlaps:

- **U1 (What it is) → DS1/PA2 (How it works).** U1 gives the one-sentence definition + mental model. How-it-works must **explain the internal mechanism that makes that definition true** - the memory layout, the invariant, the address arithmetic - _not_ re-state the definition in more words. If How-it-works opens by re-defining the structure, it failed.
- **AL1 (Intuition) → AL2 (How it works / worked example).** AL1 = plain-language _why it works_ (the idea). AL2 = the concrete _trace_ on real input. AL2 must not re-explain the intuition; it shows it happening step by step.

Each section earns its place by adding a layer the previous one didn't.

### Competitive-programming coverage (where CP lives in an article)

CP-readiness isn't one section - it's distributed, and each home is deliberate:

- **`## Constraints & approach`** (AL10 / PA10) - the CP cornerstone: input-size → expected complexity → which approach. Algorithms and patterns only.
- **`## Comparison`** (DS8 / AL9) - the scannable this-vs-rivals table.
- **Gotchas (DS7 / AL6)** - CP-flavored traps: overflow, 1-vs-0 indexing, fast I/O, modular arithmetic.
- **Python (U4)** - contest-velocity stdlib (`bisect`, `Counter`, `heapq`) where it replaces hand-rolled logic.
- **Practice problems (U6/U22-25)** - canonical contest staples, not generic filler, including contest tools the structure/pattern unlocks (prefix sums, difference array, monotonic stack, …) written as full worked entries. Single problems section for every article type, including Patterns - the sole home for CP-primitive-style content, no separate section.

---

## Universal params - every article

Write all of these regardless of section.

| #   | Param                              | What to write                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| U1  | One-line definition + mental model | Open "What it is" with a single-sentence definition **and** a mental model / analogy (a comparison to something familiar, or a bolded one-liner). Not a wall of text.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| U2  | Complexity stated                  | Give time and space complexity explicitly in Big-O. Never leave it implied. **Recursive algorithms:** the space complexity must include the call-stack term (e.g. O(n) for unbounded recursion depth, O(log n) for balanced divide-and-conquer) - do not state O(1) space for a recursive solution unless the recursion is explicitly tail-call-eliminated or converted to iteration, and say so.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| U3  | When to use / when not             | Concrete interview decision cues - _when to reach for this vs an alternative_ - naming **≥1 alternative**. Not "use it when you need it."                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| U4  | Python present + idiomatic         | Real, runnable-shape Python: type hints, comprehensions, `enumerate`, unpacking, stdlib (`collections.deque`, `heapq`) where natural. Not transliterated C. Import every module used at the top of the block. **Contest velocity:** where a stdlib one-liner replaces hand-rolled logic, show it - `bisect.bisect_left`, `collections.Counter`, `heapq.nlargest`, `itertools.accumulate`. CP is speed-of-coding; the from-scratch version is for the Implementation section, the stdlib shortcut is what you'd actually type in a contest. |
| U26 | Python - stdlib scope restriction  | **Implementation section rule:** the `## Implementation` Python must show the algorithm from scratch - do **not** use `bisect`, `sortedcontainers`, or any stdlib that hides the mechanism being taught (e.g. descent logic in a tree, partition in quicksort). Those abstractions belong in practice-problem solutions where contest velocity matters, not in the implementation that teaches the structure. **This carve-out has a limit: a practice-problem solution must never import a stdlib/library call that *is* the exact mechanic the problem asks the reader to produce** - a permutations/combinations problem must not solve itself with `itertools.permutations`, a sorting problem must not call `sorted()`/`.sort()`, a search problem must not call `bisect`. If the problem's core ask is "produce X", the solution must produce X by hand; stdlib shortcuts stay legal for incidental plumbing around that core ask (e.g. `collections.Counter` inside a two-pointer solution is fine - counting isn't the tested mechanic). |
| U5  | Pseudocode present + ≠ Python      | Required for any **non-trivial logic** (real loop / recursion / invariant), in CLRS style (see Format conventions). Skip **only** for a genuinely trivial op the ops table already conveys (e.g. hash-set membership) - and the rater needs that skip justified, so flag it inline.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| U6  | Practice problems - entry format   | A `## Practice problems` section with **≥3 problems**, each a **full worked entry** - the article's sole home for problem-solving content, across every article type. Per problem, in this order: **(1) Problem statement** - 2-3 sentences, someone who's never seen it understands the task. **(2) Worked examples** - 2-3 examples (2 for simpler problems, 3 for complex ones), as a nested list, not a table: `- **Example N**` with a nested `- **Input:** ... \| **Output:** ...` bullet and a nested `- **Explanation:** ...` bullet (always included, one sentence, even when the mapping looks self-evident). **(3) Constraints** - input bounds, where they shape the approach. Statement, Examples, and Constraints are always visible on the page (app-rendered - not behind any reveal control). **(4) Approach** - prose, the reasoning/insight for _why_ this technique fits this specific problem (distinct from PA10's article-level "does this pattern apply at all" framing - this is problem-specific). **(5) Solution** - a short, runnable Python solution (idiomatic, per U4). **(6) Complexity** - one line immediately after the code block, not its own subsection. Approach, Solution, and Complexity are hidden by default in the rendered app behind one reveal control per problem - write them as normal always-present markdown; the hide/reveal is a rendering behavior, not something the article source encodes. Tag the pattern and cross-link with a relative `.md` link where a target exists. **Favor canonical contest/interview staples** over generic made-up ones (arrays: Subarray Sum Equals K, Trapping Rain Water, Next Permutation - not "sum an array") - but an LC number is **not mandatory**: a genuinely good problem without one (classic/textbook, CP-primitive-style) is still eligible, just omit the `(LC NNN)` tag for that entry. The full per-entry markdown shape: `### N. Problem Name\n\n**Problem.** ...\n\n- **Example 1**\n  - **Input:** ... \| **Output:** ...\n  - **Explanation:** ...\n\n**Constraints.** ...\n\n**Approach.** ...\n\n```python\n...\n```\n\n**Complexity.** O(...) time, O(...) space.\n\n**Duplicate problems:**\n- Name (LC NNN) - one sentence.` (Duplicate problems is U25, below - shown here only to show where it sits in the entry.) |
| U22 | Practice problems - title/statement neutrality | **Titles must not leak the technique or solution** - a title names the problem, never the approach that solves it (e.g. "Next Greater Element", not "Next Greater Element via Monotonic Stack"); the reader should have to think before the section reveals how to solve it. **The problem statement text carries the same rule** - describe only the task (inputs, outputs, the constraint to satisfy), never the technique that solves it. Do not name the data structure, algorithm, or pattern anywhere in the statement or its examples/explanations (e.g. write "compute how much rain water is trapped between the bars" - not "...this is the two-pointer workhorse: no extra array, just two indices closing inward"). The technique surfaces for the first time in **Approach**, not before. |
| U23 | Practice problems - contest-tool coverage | **DS/PA contest-tool coverage lives here, not in a separate section**: work in the contest tools this structure/pattern unlocks (array → prefix sums 1D/2D, difference array, counter-array; stack → monotonic stack; queue → 0/1-BFS deque, sliding-window-max; graph → adjacency-list-with-weights trick, DSU-on-graph, Euler tour for LCA/subtree queries; sliding window → monotonic-deque for window-max; two pointers → meet-in-the-middle) as full worked entries - each needs its own genuine canonical problem to anchor the statement/examples/constraints shape, not a bare technique writeup. **Gated for the Linear DS family** (array, string, linked-list, stack, queue), **Graph**, and **Patterns** (all CP-central): at least one Practice entry must be a contest-tool problem, not just an interview staple. Advisory elsewhere - include one where a genuine contest tool exists, skip rather than manufacture filler. |
| U24 | Practice problems - distinct technique per entry | **Every problem must exercise a _distinct_ technique** - no two problems solved the same way just to pad the count; same core invariant/mechanic counts as the same technique regardless of surface framing (e.g. Container With Most Water and Trapping Rain Water both reduce to "track running max, advance the shorter side" - only one gets a full entry, the other becomes its duplicate-problems line). Distinct-technique scope is **per-article only** - do not cross-check against other articles' problem selection. Quality and coverage over quantity. |
| U25 | Practice problems - duplicate problems | **Gated ≥1, advisory 3-5:** immediately after each worked problem's complexity line, add a `**Duplicate problems:**` line listing 1-5 problems that are duplicates by solution - same core mechanic, different surface framing or constraint. Format: one entry per line, `Problem Name (LC NNN if exists) - one sentence explaining why the solution is the same.` These are _not_ full worked entries; title + one sentence only. At least one worked problem must have a duplicate-problems list (gated); having 3-5 such lists across the section is advisory. Do not list a problem as a duplicate if it requires a genuinely different algorithm - those belong as separate worked problems. |
| U7  | Format spine                       | `# Title` → `## Prerequisites` → `## Table of Contents` → body. No YAML.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| U8  | Title ↔ filename                   | H1 slugifies to the filename. (Verified by the script.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| U9  | Prerequisites format               | `[Title](./path.md) [tier]` - name and tier only, no reason sentence (the rendered chip is name + tier only; a reason never reaches the reader). HTML-comment link for not-yet-written targets. At least one prerequisite must directly support understanding. |
| U27 | Prerequisites - dependency/tier correctness | **Each listed prerequisite must be a genuine concept dependency** - the reader cannot follow this article without it - not a merely-related or same-family topic. Before listing an entry, silently check it's a real dependency, not topical adjacency; if it's only adjacency, drop the entry rather than list it. **Tier is exactly one of two values - no others:** `[Must read]` - the reader genuinely cannot follow this article without this concept; the article assumes it and will not re-explain it. `[Should read]` - materially helps (faster comprehension, fuller context, connects to a related technique) but the article is still followable without it. Do not invent a third tier (`[Recommended]`, `[Nice to have]`, `[Optional]`, etc.) - collapse anything softer than Must-read into Should-read. |
| U10 | TOC present                        | A `## Table of Contents` reflecting the headings.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| U11 | Filename convention                | Lowercase, hyphen-separated, `.md`. (Verified by the script.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| U12 | Links resolve                      | Every `[text](./path.md)` link points to a real file, cross-vertical included. (Verified by the script.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| U13 | Interview soundbite                | One memorable sentence a candidate can **say out loud** to sum up the concept (e.g. "A heap is a binary tree where the parent beats its children - great for anything 'top-K'."). Distinct from U1: U1 teaches (may run 2–3 sentences); this is the one-line spoken compression, marked as the takeaway.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| U14 | Section layering (advisory)        | Adjacent sections must **add a layer**, not restate. Check two classic overlaps before publishing: **(1) U1 → DS1/PA2:** `## What it is` gives the one-sentence definition + mental model; `## How it works` must explain the **internal mechanism** that makes that definition true - not re-state the definition in more words. If `## How it works` opens by re-defining the structure, it failed. **(2) AL1 → AL2:** `## Intuition` = plain-language _why it works_; `## How it works` = the concrete _trace_ on real input. `## How it works` must not re-explain the intuition. Self-check: read the first sentence of each section - if it says the same thing the prior section just said, cut and replace with the next layer. |
| U17 | Real-world usage + at-scale failure (advisory) | **2–3 sentences**, folded into `## When to use / when not` (or `## Variants`) - no new heading. **(1)** Name a real system where this is a workhorse, cross-linking the system-design vertical where natural (heaps → schedulers/event loops; hash tables → DB indexes; arrays → every dynamic language's `list`). **(2)** One sentence on what breaks at scale - the failure mode that only appears when n > 10⁷ or under high concurrency (array resize transient 2× memory spike; hash table rehash stall under write-heavy load; heap pointer-chasing at large n killing cache). This is the bridge a senior candidate walks across when asked "how would you scale this?" - one sentence is enough to show the path. |
| U18 | Cache behavior (advisory)          | **One sentence**, folded into the most natural existing section (DS: `## Memory layout` or `## How it works`; AL: `## Complexity derivation` or `## How it works`) - no new heading. State whether this structure is **cache-friendly or cache-hostile, and why**: contiguous layout → sequential prefetch → fast; pointer-chasing → scattered heap allocations → cache miss per hop → slow in practice even when Big-O looks equal. For DS: heap stored as flat array = cache-friendly; BST pointer tree = hostile. For AL: Dijkstra's priority-queue hops = hostile; prefix-sum scan = friendly. **DS per-family default: see the table under DS9a below** (Linear/Hash-based/Tree-heap = required, Graph = advisory). For AL and PA, required where the memory access pattern is non-trivial; skip only if genuinely irrelevant (e.g. a pure mathematical recurrence with no data structure). |
| U20 | Common misconceptions (advisory)   | **1–3 bullets**, folded into `## Gotchas / edge cases` (DS/AL) or `## Pitfalls` (Patterns) - no new heading. Each bullet corrects a **wrong mental model** a candidate may hold going into an interview, not an implementation bug. Distinction: gotchas = "you coded it right but missed this edge case"; misconceptions = "you reached for this tool with a false belief about what it does." Examples: "Dijkstra works with negative weights" (it doesn't - the finalized-node assumption breaks); "a heap gives you sorted iteration" (it doesn't - repeated pop is O(n log n) heapsort, not a free sorted view); "Union-Find detects cycles in directed graphs" (wrong tool - DSU tracks undirected connectivity only); "DP always needs a 2D table" (1D with rolling arrays often suffices). Write only misconceptions that are (a) wrong, (b) plausible enough that a candidate would hold them, and (c) not already covered as a gotcha. Skip if no genuine misconceptions exist for this topic - do not manufacture filler. |
| U21 | Fenced code blocks - language tag  | Every fenced code block (` ``` `) must declare a language immediately after the backticks - ` ```python `, ` ```text ` for CLRS-style pseudocode, ` ```bash ` for shell, etc. A bare ` ``` ` fence with no tag is a format violation, no exceptions. Enables correct syntax highlighting and lets the deterministic checker catch it (see [scripts/dsa-check.sh](../../../scripts/dsa-check.sh)). |

---

## Section blocks & family blocks (per category)

Each category's params (DS/AL/PA) and family blocks live in its own file, not here - open only the one matching your target folder: [dsa-writer-ds.md](./dsa-writer-ds.md), [dsa-writer-algorithm.md](./dsa-writer-algorithm.md), [dsa-writer-pattern.md](./dsa-writer-pattern.md). Each category file also carries its own **Headings list** - the ordered structural spine for that article type.

**Family choice - primary subject, not techniques touched in passing.** Sorting is **Search/divide** even though heap-sort references a heap; Backtracking is **Recursive/build** even though it recurses. When genuinely split, pick the family whose block you cover at most depth.

**Non-comparison / no-clean-fit algorithms.** A few algorithms don't sit in any family listed in dsa-writer-algorithm.md - non-comparison sorts (counting/radix/bucket) and iterative loop-invariant sorts (insertion/selection). Two rules:

- **Non-comparison sorts** use the **Distribution** family block (`## Key & distribution`) - they genuinely share a mechanism (index/bucket by key value, not compare), so it's a real family, not a dumping ground.
- **Anything still without a fit** (e.g. insertion sort's incremental loop) picks the **nearest** family, repurposes its heading to the algorithm's actual structure (loop state, not a recurrence), and **flags the stretch in a `> Family note` blockquote** in the article so the rater doesn't penalize a forced fit. The depth must live somewhere real (the invariant, the cost analysis) - the family label is bookkeeping, not the substance.
