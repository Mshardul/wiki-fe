# Practice Problems Audit (wiki-fe) — Data Structures only

Generated by practice-problems-audit, scoped to Data Structures only (Patterns and Algorithms not covered in this run — see `docs/_meta/plans/dsa-worked-problems-dedup.md` for those types' prior audit history). Content-quality review of every `## Practice problems` section against dsa-writer.md's distinct-technique constraint and topic-fit bar — not a sync check against the hand-written `dsa-worked-problems-dedup*-inventory.md` files.

Run against: `docs/_meta/practice-problems-index.md` generated 2026-07-31, 98 files / 315 entries total (DS subset: 29 files with a `## Practice problems` heading, 3 stubs skipped — `circular-buffer.md`, `hash-set.md`, `segment-tree.md` — 26 real files audited).

## Data Structures

## `content/dsa/data-structures/array.md`

### Pass 1 — full entries

- **Trapping Rain Water** — KEEP. Converging two-pointer with running max on each side, advance the shorter side. Genuinely distinct from every other entry in this file. Code hand-traced against both worked examples (6, 9) - correct. Complexity O(n)/O(1) matches code.
- **Next Permutation** — KEEP. Pure index arithmetic (pivot scan + suffix reverse), no auxiliary structure - distinct mechanic from the other three. Code hand-traced against both examples ([1,3,2], [1,2,3]) - correct.
- **Maximum Subarray** — KEEP. Kadane's DP-over-endpoints, distinct from the two-pointer and index-manipulation entries. Code hand-traced against both examples (6, 1) - correct.
- **Minimum Size Subarray Sum** — KEEP. Sliding window (monotonic expand-then-contract), distinct from Kadane's and from the two-pointer converge. Code hand-traced against both examples (2, 1) - correct.

No REASSIGN or NEEDS-DECISION calls. Container With Most Water correctly cited as Trapping Rain Water's duplicate (not a rival entry), matching the dedup plan's own canonical precedent.

### Pass 2 — duplicate-problems citations

- Under **Trapping Rain Water**: **Container With Most Water (LC 11)** — KEEP, verified. Count: 1 — acceptable, matches the dedup plan's own canonical example (decision 4).
- Under **Next Permutation**: **Previous Permutation With One Swap (LC 1053)** — KEEP, verified. Count: 1 — acceptable, thin natural pool.
- Under **Maximum Subarray**: **Maximum Sum Circular Subarray (LC 918)**, **Maximum Product Subarray (LC 152)** — both KEEP, verified. Count: 2 — acceptable, below 3-5 target but justified.
- Under **Minimum Size Subarray Sum**: **Minimum Window Substring (LC 76)** — KEEP, verified. Count: 1 — acceptable.

### Facts/correctness notes

- No LC-number mismatches; all 4 citations verified fresh via WebSearch.
- All 4 code blocks hand-traced against every worked example — zero discrepancies.

---

## `content/dsa/data-structures/avl-tree.md`

### Pass 1 — full entries

- **Insert into an AVL tree** — KEEP. Rebalance-on-the-way-up via 4 rotation cases.
- **Validate height-balanced** — KEEP. Bottom-up tree-DP, purely diagnostic, distinct from insert/delete.
- **Build a balanced BST from sorted data** — KEEP. Static midpoint-recursion, explicitly contrasted against AVL's dynamic approach.
- **AVL delete with rebalance-on-removal** — KEEP. Multi-ancestor rebalancing, structurally distinct from insert (continues checking every ancestor vs stopping at first fix). Confirms the dedup plan's own record that this entry replaced a prior fake-distinct "count rotations" wrapper.

No REASSIGN or NEEDS-DECISION calls.

### Pass 2 — duplicate-problems citations

- Under **Insert into an AVL tree**: **Balance a Binary Search Tree (LC 1382)** — KEEP, verified. Count: 1 — acceptable.
- Under **Build a balanced BST from sorted data**: **Convert Sorted List to Binary Search Tree (LC 109)** — KEEP, verified. Count: 1 — acceptable.
- **Validate height-balanced** and **AVL delete** have zero citations — U6 floor cleared by the other two entries; omissions read as deliberate, not under-researched.

### Facts/correctness notes

- No LC-number mismatches. AVL delete entry independently re-executed against the article's own Implementation-section helpers — both worked examples match exactly (this is the delete/rebalance logic the dedup plan flagged as randomized-stress-tested before landing; re-verification here confirms it held).
- Complexity claims correct throughout, including the recursive-space term.

---

## `content/dsa/data-structures/b-plus-tree.md`

### Pass 1 — full entries

- **Range query on a sorted structure** — KEEP. Linked-leaf-list walk, B+ tree's defining superpower over a plain B-tree.
- **Design an index for a database column** — KEEP. System-design reasoning entry, legitimate mixed-format pattern (also used in b-tree.md).
- **Insert-with-leaf-split (copy-up)** — KEEP. Copy-vs-move asymmetry is the file's own stated "critical difference." Code and both worked examples independently re-executed against the file's own Implementation code — exact match.

No REASSIGN or NEEDS-DECISION calls. Only 3 entries (at the floor, not a violation) — a natural 4th candidate (B+ tree delete/leaf-underflow-merge) is noted as a coverage gap, not a defect.

### Pass 2 — duplicate-problems citations

- Under **Range query on a sorted structure**:
  - **Count of numbers in a range (LC 2250)** — **REMOVE (hard fact mismatch)**. WebSearch confirms real LC 2250 is "Count Number of Rectangles Containing Each Point" — an unrelated 2D geometry problem. No clean canonical LC replacement found via search.
  - **Find first and last position of element in sorted array (LC 34)** — KEEP, verified.
  - Count: 2 nominal, 1 confirmed-good after removing the bad citation.
- Under **Design an index for a database column**: 1 design-style citation, no LC number — KEEP, appropriate for a reasoning-only entry.
- **Insert-with-leaf-split** has zero citations — U6 floor cleared elsewhere; a natural cross-file duplicate exists in b-tree.md's own split entry but cross-file citations aren't required per the plan's scoping rule.

### Facts/correctness notes

- **LC-number mismatch (hard fail)**: entry 1's duplicate-problems line cites the wrong real problem for LC 2250 — needs a human decision (drop or replace).
- `_split_child`/`insert`/`range_query`/`search` code independently re-executed against both worked examples — exact match, including the copy-up-not-move semantics claim.

---

## `content/dsa/data-structures/b-tree.md`

### Pass 1 — full entries

- **Why B-trees for databases** — KEEP. Reasoning-only, no-code by design.
- **B-tree search** — KEEP. Multi-key-node descent, the canonical traversal. Code hand-traced — correct.
- **Choose the order for a disk block** — KEEP. Pure sizing arithmetic; independently re-derived (m≈256, height≈4) — correct.
- **B-tree vs B+-tree for range scans** — KEEP. Reasoning-only comparison, content accurate, no cross-file contradiction with b-plus-tree.md.
- **Insert-with-node-split (median push-up)** — KEEP. Canonical split-on-overflow. Code and both worked examples independently re-executed — exact match.

No REASSIGN or NEEDS-DECISION calls on Pass 1 — file's own framing ("five problems, no two the same") holds up.

### Pass 2 — duplicate-problems citations

**Zero `**Duplicate problems:**` lines anywhere in this file** — all 5 entries. **NEEDS-DECISION**: (a) leave as-is, since B-trees genuinely have almost no LC-numbered presence (the article's own "Contest velocity" note supports this), or (b) add at least one explicit "none found" justification note per the pattern used elsewhere in the corpus, since the rater's mechanical U6 gate check would currently flag this section as failing the "≥1 worked problem must have a duplicate-problems list" floor with no in-file explanation. Recommend at minimum one explicit "none found, reason: X" note rather than silence.

### Facts/correctness notes

- Gap flagged above as NEEDS-DECISION, not a hard defect — but inconsistent with every other file in this chunk (all of which have ≥1 citation or an explicit "none found" note).
- All code hand-traced across all 5 entries where code exists — zero discrepancies. No LC citations exist to verify.

---

## `content/dsa/data-structures/binary-search-tree.md`

### Pass 1 — full entries

- **Validate Binary Search Tree** — KEEP. Bounded-interval recursion.
- **Kth Smallest Element in a BST** — KEEP. Iterative in-order with early-exit counting.
- **Lowest Common Ancestor of a BST** — KEEP. Ordering-based split-point shortcut, correctly distinguished from general-binary-tree LCA (cross-file, out of scope but cross-reference reads accurate).
- **Insert into a BST** — KEEP. Recursive-descent-to-null-child.
- **Convert Sorted Array to BST** — KEEP. Divide-and-conquer balanced construction, explicit contrast with incremental insert.
- **Delete Node in a BST** — KEEP. Two-child successor-replacement. Both worked examples hand-traced against the file's own code — exact match.

All 6 techniques genuinely non-overlapping. No REASSIGN or NEEDS-DECISION calls.

### Pass 2 — duplicate-problems citations

- Under **Kth Smallest**: **Binary Search Tree Iterator (LC 173)** — KEEP, verified. Count: 1 — acceptable.
- Under **LCA of a BST**: **Lowest Common Ancestor of a Binary Search Tree III (LC 1650)** — KEEP underlying logic, but **title inaccuracy flagged**: real LC 1650 title is "...of a Binary Tree III" (no "Search"). Number and mechanic correct, title text wrong. Count: 1 — acceptable.
- Under **Convert Sorted Array to BST**: **Convert Sorted List to Binary Search Tree (LC 109)** — KEEP, verified, consistent with avl-tree.md's identical citation.
- Under **Delete Node in a BST**: **Delete Leaves With a Given Value (LC 1325)** — KEEP, verified, honestly self-hedged as "related but distinct."
- **Validate BST** and **Insert into a BST** have zero citations — U6 floor cleared by the other four; both omissions read as defensible.

### Facts/correctness notes

- **Title inaccuracy**: entry 3's duplicate-problems line should read "Lowest Common Ancestor of a Binary Tree III (LC 1650)" — currently has an incorrect "Search" inserted. Lower severity than b-plus-tree.md's LC 2250 issue (right problem/number, wrong title text).
- All 6 code blocks hand-traced — zero discrepancies. Cross-links to binary-tree.md and this file's own Implementation section verified to exist.

---

## `content/dsa/data-structures/binary-tree.md`

### Pass 1 — full entries

- **Maximum Depth of Binary Tree** — KEEP. Post-order recurse-and-combine.
- **Binary Tree Level Order Traversal** — KEEP. BFS with queue, distinct from #1 (iterative, level-boundary snapshot).
- **Invert Binary Tree** — KEEP. Recursive child-swap, no aggregation — legitimate distinct primitive despite shallowness.
- **Diameter of Binary Tree** — KEEP. Tree DP, "return one thing, track another via side-channel."
- **Lowest Common Ancestor** — KEEP. Search-and-bubble-up at the split point.

All 5 genuinely distinct (DFS-combine / BFS-level / in-place-mutate / tree-DP-two-values / search-and-bubble). No topic-fit misses. No REASSIGN/NEEDS-DECISION.

### Pass 2 — duplicate-problems citations

- Under **Maximum Depth**: **Minimum Depth (LC 111)** — KEEP, verified. Count: 1 — thin but acceptable given file-wide distribution.
- Under **Level Order Traversal**: **Zigzag Level Order (LC 103)**, **Average of Levels (LC 637)** — both KEEP, verified. Count: 2 — acceptable.
- Under **Diameter**: **Binary Tree Maximum Path Sum (LC 124)** — KEEP, verified (confirmed Hard difficulty, confirmed shared tree-DP shape). Count: 1 — acceptable, thin natural pool.
- Under **LCA**: **LCA of a Binary Tree II (LC 1644)** — KEEP, verified. Count: 1 — acceptable.
- **Invert Binary Tree** has zero citations — genuine "none found" case (famously singular technique), not under-research.

### Facts/correctness notes

- No issues found. All 5 entries' code and complexity claims verified sound.

---

## `content/dsa/data-structures/bloom-filter.md`

### Pass 1 — full entries

- **Design a Web Crawler URL Deduplication System** — KEEP. Canonical sizing-under-budget problem.
- **First Missing Positive** — KEEP. Deliberate counter-example ("NOT solved by a bloom filter," included to sharpen the "when NOT to use" instinct) — legitimate teaching device, same pattern as deque.md's Sliding Window Median entry.
- **Design a Counting Bloom Filter with Delete** — KEEP. Distinct mechanic: saturating counters + deletion + overflow trap.
- **Design a Spell Checker** — KEEP (borderline). Static load-once + FP-asymmetry framing differs from entry 1's dynamic-mutation framing, but leans close.

**NEEDS-DECISION**: Entry 1 (Web Crawler Dedup) and Entry 4 (Spell Checker) share the same core bloom-filter-sizing math and near-identical bit-array code shape — only the word-problem framing differs. Tradeoff: **(A) keep both** — they train genuinely different practical judgment (hard-memory-ceiling-with-mutation vs target-FP-with-asymmetry-reasoning), mirroring how sizing actually gets asked two ways in interviews; **(B) collapse** — per the dedup plan's own Container-Water/Trapping-Water precedent, "same formula, same code shape, different word-problem" should collapse to one entry with the other demoted to a duplicate-problems line. This is a closer call than any other pair in this chunk (both math *and* code skeleton are near-identical here, unlike the deque.md/bloom-filter.md counter-example entries which add a genuinely new mechanic). Recommend a human call.

### Pass 2 — duplicate-problems citations

- All 4 entries' citations (2 each, all design-style/no-LC-number) — KEEP, thin canonical LC pool for bloom-filter-flavored design problems is a legitimate reason, not under-research.

### Facts/correctness notes

- All sizing math across entries 1, 3, 4 independently re-derived — all check out.
- Entry 3's counting-BF code (`add`/`remove`/`__contains__`) hand-traced against all 3 worked examples — exact match, saturation guard and remove-guard both verified correct.
- Entries 1 and 4's near-identical code/math shape is the NEEDS-DECISION item above, not a hard fail.

---

## `content/dsa/data-structures/deque.md`

### Pass 1 — full entries

- **Sliding Window Maximum** — KEEP. Canonical decreasing-monotonic-deque-of-indices. Code hand-traced — exact match on both examples.
- **Design Circular Deque** — KEEP. Ring-buffer/wrap-arithmetic, distinct invariant from #1.
- **Shortest Subarray with Sum at Least K** — KEEP. Increasing-monotonic-deque-over-prefix-sums — different invariant and pop conditions from #1, correctly self-noted as "a distinct twist." Code hand-traced against all 3 examples — exact match.
- **Sliding Window Median** — KEEP. The article's own explicit "deque is NOT enough" counter-example, defines the deque's boundary — legitimate pedagogical device (parallel to bloom-filter.md's First Missing Positive).

All 4 genuinely distinct. No topic-fit misses among the 3 that use a deque; #4 is deliberately off-structure by design.

### Pass 2 — duplicate-problems citations

- Under **Sliding Window Maximum**: **Jump Game VI (LC 1696)**, **Constrained Subsequence Sum (LC 1425)** — both KEEP, verified as canonical monotonic-deque-DP solutions. Count: 2 — acceptable.
- Under **Design Circular Deque**: **Design Circular Queue (LC 622)** — KEEP, verified. Count: 1 — acceptable.
- **Shortest Subarray with Sum at Least K**: zero citations. Flagged as **likely under-researched** (not "genuinely dup-free") — the prefix-sum+monotonic-deque combination has plausible relatives that weren't cited.
- Under **Sliding Window Median**: **Find Median from Data Stream (LC 295)** — KEEP, verified, honestly framed as "the simpler version of itself."

### Facts/correctness notes

- **Entry 3 omits its own LC number** (LC 862, "Shortest Subarray with Sum at Least K") — a well-known exact match, looks like an oversight rather than a deliberate no-LC-number choice.
- All code for entries 1-3 hand-traced and exact-match. Entry 4 skimmed clean (well-established two-heap+lazy-deletion pattern).

---

## `content/dsa/data-structures/dynamic-array.md`

### Pass 1 — full entries

- **Implement a Dynamic Array from Scratch** — KEEP. Grow/shrink resize policy, 1/4-not-1/2 shrink-threshold anti-thrashing trap. Code hand-traced — exact match.
- **O(1) Removal at an Arbitrary Index** — KEEP. Swap-with-last-then-pop, distinct from entry 5 (index vs value-based deletion).
- **Amortized Copy-Count Walkthrough** — KEEP. Meta/proof-style entry (aggregate-method proof-by-simulation), distinct skill from entry 1's build-focus. Code hand-verified: totals match stated bound.
- **Growth Factor Comparison** — KEEP. Geometric vs fixed-increment growth policies, demonstrates O(n²) failure mode. Distinct from entry 3 (comparative analysis vs single-policy proof). Code hand-verified — exact match.
- **Insert Delete GetRandom O(1)** — KEEP. Value-based deletion via index map, distinct from entry 2.
- **Min Stack** — KEEP. Parallel auxiliary buffer, no resize/swap discussion at all.
- **Implement Queue using Stacks** — KEEP. Two-buffer amortized pour, different amortized argument shape from entries 1/3/4.

All 7 genuinely distinct. Entries 3/4 are the closest pair (both "prove/measure the amortized claim") but exercise different skills (single-policy proof vs comparative policy analysis) — defensible, not padding, given dynamic-array's whole point is amortized analysis.

### Pass 2 — duplicate-problems citations

- Under entry 1: 1 no-LC-number design-style citation — KEEP, canonical/only real duplicate.
- Under entry 5 (Insert Delete GetRandom): **LC 381** (duplicates-allowed variant) — KEEP, verified.
- Under entry 6 (Min Stack): 1 no-LC-number citation — KEEP; correctly avoided over-citing LC 716 "Max Stack" since that problem needs an extra `popMax()` operation this entry's technique doesn't cover.
- Under entry 7 (Queue using Stacks): **Implement Stack using Queues (LC 225)** — KEEP, verified genuine mirror-image.
- Entries 2, 3, 4 (O(1) removal, amortized-copy-count, growth-factor) have zero citations — defensible for 3/4 (meta/proof-style, no natural LC twin); entry 2 flagged as a soft under-research gap.

### Facts/correctness notes

- All numeric outputs across entries 1, 3, 4 independently re-run and matched exactly.
- Entry 4's `cap = max(cap + 1, int(cap * factor))` anti-stall guard verified load-bearing and correct (without it, `int(1*1.5)=1` would permanently stick capacity at 1).
- Complexity lines all consistent with actual code behavior.

---

## `content/dsa/data-structures/fenwick-tree.md`

### Pass 1 — full entries

- **Range Sum Query - Mutable** — KEEP. Textbook point-update/range-sum baseline. Code hand-traced — exact match.
- **Count of Smaller Numbers After Self** — KEEP. BIT-as-order-statistics (value-indexed, rank-based), distinct from entry 1's position-indexed sum tracking. Code hand-traced — exact match.
- **Range Sum Query - Range Update and Range Sum** — KEEP. Two-BIT range-update/range-query trick, distinct dual-BIT correction-term algebra. Code hand-traced (including the article's own CP-primitives `RangeBIT` class) — exact match.

All 3 genuinely distinct BIT use shapes. No topic-fit misses. Low entry count (3) is a legitimate CP-primitives-vs-Practice-problems boundary call, not a gap.

### Pass 2 — duplicate-problems citations

- Under entry 1: **Range Sum Query 2D - Mutable (LC 308)** — KEEP, verified. **My Calendar III (LC 732)** — KEEP but **mildly optimistic framing flagged**: LC 732 is more commonly solved with segment-tree-with-lazy or an ordered-map sweep in mainstream solutions; the BIT-based approach is valid but not the "default." Count: 2 — acceptable.
- Under entry 2: **Reverse Pairs (LC 493)**, **Count of Range Sum (LC 327)** — both KEEP, verified as valid alternative approaches (not always the mainstream one, but genuinely valid). Count: 2 — acceptable.
- Under entry 3: **Range Addition (LC 370)** — KEEP, verified. Citation is unusually self-aware, explicitly notes the two-BIT machinery is "overkill" for this simpler problem — good, honest reasoning. Count: 1 — acceptable, legitimately thin pool.

### Facts/correctness notes

- No LC-number/logic errors found. All 3 entries' code independently re-executed, all worked examples matched exactly.
- Cross-links to the file's own CP-primitives section verified to exist and be consistent.

---

## `content/dsa/data-structures/graph.md`

### Pass 1 — full entries

- **Number of Islands (LC 200)** — KEEP. BFS/DFS connected-components on implicit grid graph.
- **Clone Graph (LC 133)** — KEEP. BFS + hashmap original→clone, distinct build-a-copy-while-traversing mechanic.
- **Course Schedule (LC 207)** — KEEP. Three-color DFS cycle detection/topo-sort family, only cycle-detection entry in the file.
- **Network Delay Time (LC 743)** — KEEP. Weighted-shortest-path via Dijkstra — the file's own Approach prose correctly self-identifies this as filling a coverage gap named in "When to use" but not otherwise exercised.

All 4 genuinely distinct (components / clone-via-BFS / cycle-detection / weighted-shortest-path), all four genuinely require a graph structure. No REASSIGN/NEEDS-DECISION.

### Pass 2 — duplicate-problems citations

- Under Number of Islands: **Flood Fill (LC 733)**, **Max Area of Island (LC 695)**, **Count Sub Islands (LC 1905)** — all KEEP, verified. Count: 3, in-range.
- Under Clone Graph: **Copy List with Random Pointer (LC 138)**, **Graph Valid Tree (LC 261)**, **Pacific Atlantic Water Flow (LC 417)** — all KEEP, verified. Count: 3, in-range.
- Under Course Schedule: **Course Schedule II (LC 210)**, **Find Eventual Safe States (LC 802)**, **Alien Dictionary (LC 269)** — all KEEP, verified. Cross-reference note: LC 802 also appears as a full entry in `strongly-connected-components.md` (Algorithms, out of scope) under a different technique (Tarjan+condensation) — documented non-violation precedent per the dedup plan. Count: 3, in-range.
- Under Network Delay Time: **Path with Minimum Effort (LC 1631)**, **Cheapest Flights Within K Stops (LC 787)** — both KEEP, verified. Count: 2, slightly below target but both strong. Cross-reference: `heap.md`'s own Dijkstra entry (this same run) also cites LC 1631 under a different parent entry — legitimate, not a same-file conflict.

### Facts/correctness notes

- No LC-number/title mismatches — all 15 citations verified.
- No logic errors, off-by-ones, or complexity mismatches in any of the 4 entries.

---

## `content/dsa/data-structures/hash-table.md`

### Pass 1 — full entries

- **Two Sum — complement lookup** — KEEP. Purest form of the seen-map/complement-lookup primitive.
- **Group Anagrams — canonical key** — KEEP. Bucketing on a derived signature, distinct from raw-value lookup.
- **Longest Consecutive Sequence — set membership** — KEEP. `x-1 not in s` run-start guard, distinct amortized-O(n) mechanic.
- **Subarray Sum Equals K — prefix sum + hashing** — KEEP. Article explicitly and correctly justifies belonging on hash-table.md over array.md (hashmap load-bearing due to negative values).
- **First Unique Character — frequency map** — KEEP. Plain two-pass counting, simplest but legitimately distinct.

All 5 genuinely distinct per the file's own framing. No REASSIGN/NEEDS-DECISION.

### Pass 2 — duplicate-problems citations

- Under Two Sum: **Two Sum IV - BST (LC 653)**, **4Sum II (LC 454)** — both KEEP, verified. Count: 2 — acceptable.
- Under Group Anagrams: **zero citations, no justification note** — flagged as **under-researched, not genuinely dup-free**: Group Shifted Strings is a well-known close duplicate that wasn't cited.
- Under Longest Consecutive Sequence: **zero citations** — more defensible (mechanic is fairly singular in LC's common set) but still lacks the required justification note.
- Under Subarray Sum Equals K: **Contiguous Array (LC 525)**, **Subarray Sums Divisible by K (LC 974)** — both KEEP, verified. Count: 2 — acceptable, a third (Continuous Subarray Sum LC 523) was available but not cited.
- Under First Unique Character: **zero citations** — flagged as **under-researched**: this is one of the most duplicate-rich mechanics in LC's common set, leaving it uncited without justification is a real gap.

**File-level summary**: 3 of 5 entries have zero citations and none carry an explicit "none found" note. **Discrepancy flagged**: the dedup plan claims this file was one of 8 fixed in a dedicated 2026-07-31 research pass ("37 entries checked, 20 got citations, 17 got explicit none-found notes") — but the file's current shipped content shows only 2 of 5 entries with citations and 3 of 5 with neither citation nor note. The claimed fix does not appear to be fully present in the file as it stands today.

### Facts/correctness notes

- No LC-number mismatches among the 4 citations that exist.
- No logic/complexity errors across all 5 entries.
- **Coverage-gap discrepancy** flagged above — recommend re-checking this file specifically against the plan's claimed completion state.

---

## `content/dsa/data-structures/heap.md`

### Pass 1 — full entries

- **Kth Largest Element in a Stream** — KEEP. Bounded min-heap keyed on raw value. Per the codebase's own precedent on `top-k-elements.md` (Patterns, out of scope but cited as precedent), streaming-vs-batch alone doesn't create distinctness, but raw-value-vs-derived-key does — this entry (raw value) is correctly distinct from entry 2 (frequency key).
- **Top K Frequent Elements** — KEEP. Frequency-keyed size-K heap — distinct per the same precedent.
- **Merge K Sorted Lists** — KEEP. Heap of K frontier pointers, genuinely distinct from bounded top-K heaps. Correctly cross-references its own dedicated `k-way-merge.md` (Patterns) entry rather than duplicating depth.
- **Find Median from Data Stream** — KEEP. Two balanced heaps, distinct mechanic.
- **Swim in Rising Water** — KEEP. Dijkstra-style heap shortest path, explicitly self-identified as filling the file's own coverage gap.

All 5 genuinely distinct heap techniques. No REASSIGN/NEEDS-DECISION.

### Pass 2 — duplicate-problems citations

- Under Kth Largest in a Stream: **LC 215** — KEEP, verified, matches codebase precedent treating streaming/batch as duplicates. Count: 1 — thin but acceptable.
- Under Top K Frequent: **Top K Frequent Words (LC 692)**, **K Closest Points to Origin (LC 973)** — both KEEP, verified. Count: 2 — acceptable low end.
- Under Merge K Sorted Lists: **Kth Smallest in Sorted Matrix (LC 378)** — KEEP, verified, legitimately points to where the full treatment lives (`k-way-merge.md`). Count: 1 — thin.
- Under Find Median: **Sliding Window Median (LC 480)** — KEEP, verified. Count: 1 — thin.
- Under Swim in Rising Water: **Path with Minimum Effort (LC 1631)**, **Path with Maximum Probability (LC 1514)** — both KEEP, verified. Count: 2 — acceptable low end.

**File-level summary**: every entry has ≥1 citation, but 3 of 5 sit at exactly 1 — below the 2-8 range on the low end. All existing citations are precisely reasoned and verified real. A light research pass would strengthen but nothing here is wrong.

### Facts/correctness notes

- **Missing LC-number tags on two entry headings** (not a citation issue): "Merge K Sorted Lists" is LC 23 (confirmed — the article's own worked example is the canonical LC 23 example) but the heading omits the tag. "Swim in Rising Water" is LC 778 (confirmed — worked example matches) but the heading omits the tag. Cross-reference: the dedup plan's own DS inventory file records both with correct LC numbers, so the omission is specific to the shipped content, not a research gap.
- No logic/complexity errors across all 5 entries.
- Cross-reference: `lfu-cache.md` (this same run) independently cites LC 692 under its own different-technique entry — legitimate, both angles genuinely distinct.

---

## `content/dsa/data-structures/interval-tree.md`

### Pass 1 — full entries

- **My Calendar I** — KEEP. Canonical interval-tree insert+overlap-search. Code correctly implements `_overlap_search`/`_insert`/`_update_max`.
- **Find All Conflicting Meetings** — KEEP. Genuinely distinct from entry 1: multi-result stabbing query with real `max_hi`-based pruning vs entry 1's return-on-first-hit search. Code correctly verified.
- **Employee Free Time** — KEEP. Exercises the tree's in-order-sorted-traversal property, distinct from #2's pruning-based query.

All 3 genuinely distinct interval-tree-specific mechanics; matches the dedup plan's own note that this file's prior topic-fit-miss entries were already replaced. No REASSIGN/NEEDS-DECISION.

### Pass 2 — duplicate-problems citations

- Under My Calendar I: **My Calendar II (LC 731)**, **My Calendar III (LC 732)** — both KEEP, verified. Count: 2 — acceptable low end.
- Under Find All Conflicting Meetings: **Remove Interval (LC 1272)**, **Minimum Arrows to Burst Balloons (LC 452)** — both KEEP, verified, honestly scoped (partial-match citations, not overclaimed). Count: 2 — acceptable low end.
- Under Employee Free Time: **Insert Interval (LC 57)**, **Merge Intervals (LC 56)** — both KEEP, verified. Count: 2 — acceptable low end.

### Facts/correctness notes

- All 6 citations verified, no mismatches.
- No logic errors. One positive note: entry 1's approach text honestly caveats that O(log n) assumes balance, which the shown plain-BST-insert implementation doesn't guarantee under adversarial insertion order — disclosed, not silently overclaimed.

---

## `content/dsa/data-structures/lfu-cache.md`

### Pass 1 — full entries

- **LFU Cache** — KEEP. Canonical anchor entry, frequency buckets + min_freq pointer. Code correctly implements `_touch`/eviction/min_freq reset.
- **All O`one` Data Structure** — KEEP, with a code-completeness flag. Genuine technique generalization (one-sided → two-sided min/max tracking). **Code is a skeleton** — the `Bucket` class has no `inc`/`dec`/`getMaxKey`/`getMinKey` method bodies, only descriptive comments. Doesn't meet U6's "short, runnable Python solution" bar — the one clear code-completeness outlier across this file.
- **Top K Frequent Elements** — KEEP. Static bucket-sort-by-frequency, explicitly and correctly contrasted with LFU's live/incremental buckets. This directly resolves the near-duplicate the dedup plan's own audit history flagged between old entries 3/4.
- **Maximum Frequency Stack** — KEEP. Live buckets evicting by maximum frequency + LIFO tie-break, precisely distinguished from LFU's minimum+LRU tie-break.
- **LRU Cache** — KEEP, but **NEEDS-DECISION-adjacent**. Framed explicitly as an intentional A/B contrast entry. Tradeoff: keep as full entry (pedagogically valuable direct contrast, clears the distinctness bar) vs demote to a duplicate-problems-style pointer to `lru-cache.md` (the reader gets zero LFU-specific teaching value from generic `OrderedDict` boilerplate reused verbatim). No forced verdict — a judgment call about pedagogical value vs section economy.

### Pass 2 — duplicate-problems citations

- Under LFU Cache: zero citations, no justification note — defensible (LFU Cache itself is fairly singular as a design problem) but should carry an explicit note per U6.
- Under All O`one`: zero citations, no note — similarly defensible but unstated.
- Under Top K Frequent Elements: **Top K Frequent Words (LC 692)**, **Sort Characters By Frequency (LC 451)** — both KEEP, verified. Count: 2 — acceptable.
- Under Maximum Frequency Stack: zero citations, no note — **weakest of the four 0-citation entries to leave unjustified**; plausible LC-adjacent duplicates exist in the broader frequency-bucketed-live-structure family.
- Under LRU Cache: zero citations — flagged that a cross-reference note pointing to `lru-cache.md`'s fuller treatment would fit the "for contrast" framing better than silence.

**File-level summary**: only 1 of 5 entries carries citations; the other 4 have neither citations nor justification notes. Clears the gated U6 floor but well below the 3-5-entries-with-dup-lists advisory target.

### Facts/correctness notes

- **Missing LC-number tag**: "Maximum Frequency Stack" is LC 895 (confirmed — worked example matches canonical LC 895 exactly) but heading omits the tag. Same pattern as `heap.md`'s two missing tags; the dedup plan's own DS inventory records this entry correctly tagged, so again the omission is specific to shipped content.
- **Thin/non-runnable code**: entry 2's `Bucket` class — flagged above.
- No logic/complexity errors in entries 1, 3, 4, 5.

---

## `content/dsa/data-structures/linked-list.md`

### Pass 1 — full entries

- **Reverse a Linked List** — KEEP. Iterative prev/cur/nxt rewiring, code verified against a full trace.
- **Linked List Cycle II** — KEEP. Floyd's tortoise-and-hare with phase-2 reset-to-head, distinct from entry 1.
- **Merge Two Sorted Lists** — KEEP. Dummy-head splice-merge.
- **Remove Nth Node From End** — KEEP. Gap-trick two-pointer (fixed offset), distinct from Floyd's variable-rate.
- **LRU Cache** — **NEEDS-DECISION (leans REASSIGN)**. Near-identical to `lru-cache.md`'s own canonical entry 1 — same problem, same hashmap+DLL mechanic, same `OrderedDict`-based code shape, same constraints, near-identical worked examples. The dedup plan's own precedent (Subarray Sum Equals K, previously duplicated across array.md/hash-table.md) was resolved by keeping the entry in its natural home and demoting the other copy. `lru-cache.md` is unambiguously LRU Cache's natural home per the plan's own log. Tradeoff: keeping it here demonstrates the doubly-linked-list's "why doubly, not singly" lesson in situ (real pedagogical value); dropping it avoids redundancy a reader hitting both pages would notice. Recommend REASSIGN — drop the full entry, replace with a duplicate-problems line pointing to `lru-cache.md`'s canonical entry — but flagging as a human call given the in-situ teaching value.

### Pass 2 — duplicate-problems citations

- Under Reverse a Linked List: **Reverse Linked List II (LC 92)** — KEEP, verified. Count: 1 — acceptable.
- Under Linked List Cycle II: **Find the Duplicate Number (LC 287)** — KEEP, verified, confirmed as the plan's own documented relocation target (previously misplaced in array.md, correctly resolved here). Count: 1 — acceptable.
- Under Merge Two Sorted Lists: **Merge Sorted Array (LC 88)** — KEEP, verified. Count: 1 — acceptable.
- Under Remove Nth Node From End: no citation — acceptable.
- Under LRU Cache: no citation, moot pending the Pass 1 decision above.

### Facts/correctness notes

- No LC-number mismatches; all 3 cited numbers verified.
- Code in all 5 entries hand-traced, correct.
- **Cross-file duplicate note**: `lru-cache.md` and `lfu-cache.md` (this same run) both also contain their own full "LRU Cache" entries — a second/third copy of essentially the same problem across 3 files total. Flagging for reconciliation across all three at once rather than fixing `linked-list.md` in isolation.

---

## `content/dsa/data-structures/lru-cache.md`

### Pass 1 — full entries

- **LRU Cache** — KEEP (canonical home) — see cross-file note under `linked-list.md` above. `OrderedDict`-based code correct.
- **LFU Cache** — KEEP. Genuinely distinct technique, explicitly framed as the intentional contrast entry. Code hand-traced against the worked example — matches exactly.
- **LRU Cache with TTL** — KEEP. Adds wall-clock expiry gate before splice, distinct eviction semantics (lazy-expire-on-access). Code correct.

No REASSIGN/NEEDS-DECISION within this file's own entries (the cross-file overlap with `linked-list.md` is flagged there).

### Pass 2 — duplicate-problems citations

**Zero `**Duplicate problems:**` lines across all 3 entries** — a **gated-floor violation**. This reads as genuinely under-researched, not "no duplicates exist": LFU Cache (entry 2's own problem) has real LC-known duplicates, and design-cache-family problems (e.g. "All O`one` Data Structure," LC 432) share the map-plus-ordered-structure composition with entry 3. Recommend adding at least 1 citation.

### Facts/correctness notes

- Entry 2 ("LFU Cache") cites no LC number in its own statement despite being LC 460 — worth adding for consistency with the rest of the file's convention.
- No code/approach mismatches; complexity claims verified.

---

## `content/dsa/data-structures/queue.md`

### Pass 1 — full entries

- **Implement Queue using Stacks** — KEEP. Amortized-transfer two-stack technique. Code correct, verified via aggregate-cost argument.
- **Number of Recent Calls** — KEEP. Sliding-window-over-time queue, distinct from entries 1 and 3.
- **Sliding Window Maximum** — KEEP. Monotonic decreasing deque, canonical enough to warrant its own entry despite Patterns-vertical overlap (correctly cross-linked).
- **Rotting Oranges** — KEEP. Multi-source BFS, distinct graph-traversal mechanic. Code correctly marks-on-enqueue to prevent double-enqueuing.
- **Design Circular Queue** — KEEP. Ring-buffer with explicit size-tracking, `(front+size)%cap` addressing avoids front==back ambiguity.

All 5 genuinely distinct. No REASSIGN/NEEDS-DECISION.

### Pass 2 — duplicate-problems citations

- Under entry 1: **Implement Stack using Queues (LC 225)** — KEEP, verified, honest citation reasoning. Count: 1 — acceptable.
- Under Sliding Window Maximum: **Shortest Subarray with Sum at Least K (LC 862)** — KEEP, verified. Count: 1 — acceptable.
- Under Rotting Oranges: **01 Matrix (LC 542)** — KEEP, verified, strong specific citation. Count: 1 — acceptable.
- Under Design Circular Queue: 1 no-LC-number citation (streaming first-unique-char variant) — KEEP, correctly omits an LC tag since this specific streaming variant isn't itself numbered. Count: 1 — acceptable.
- File-level: 4 of 5 entries carry a dup-line, meets the ≥1-gated floor comfortably.

### Facts/correctness notes

- No LC-number mismatches; all 3 numbers verified.
- Cross-link to `./circular-buffer.md` resolves to a real file.
- No code/complexity mismatches.

---

## `content/dsa/data-structures/red-black-tree.md`

### Pass 1 — full entries

- **Why libraries pick red-black over AVL** — KEEP. Reasoning-only entry, `Complexity: N/A` honestly justified. Height-bound math independently re-derived and confirmed correct (29 and 40 levels).
- **Verify red-black properties — black-height check** — KEEP. Recursive black-height verification, correctly exercises the article's own invariants.
- **Red-black insert fixup — recolor then rotate** — **NEEDS-DECISION**. The Solution code block is **not self-contained runnable** — a 3-line driver pointing back to the Implementation section, with the case-branching logic the Approach prose describes never actually shown in the entry itself. Tradeoff: re-pasting the full `_fixup` implementation would duplicate the Implementation section almost verbatim; but U6 requires standalone runnable code per entry, and a reader following only Practice-problems can't see the described branching. Flagged for a human call on whether pointing back to Implementation is acceptable for DS articles specifically.
- **Order-statistics with a red-black tree** — KEEP. Subtree-size augmentation for rank/select, distinct technique. Code hand-traced and correct, though explicitly labeled "conceptual" (more honest framing than entry 3's silent gap).

### Pass 2 — duplicate-problems citations

- Under Order-statistics: **Count of Smaller Numbers After Self (LC 315)** — KEEP, verified, precisely reasoned.
- **Only 1 of 4 entries carries a citation** — meets the bare gated floor but far below the 3-5 advisory target. Entries 2 and 3 plausibly have researchable duplicates that weren't cited (entry 3's fixup technique may genuinely have no clean LC duplicate, but this isn't stated as such in the article, so the omission reads as under-researched rather than deliberately justified).

### Facts/correctness notes

- Entry 3's code gap flagged above at `red-black-tree.md:390-395`.
- No LC-number mismatches (only LC 315 cited, verified correct).
- No complexity-claim mismatches across entries 1-4.

---

## `content/dsa/data-structures/skip-list.md`

### Pass 1 — full entries

- **Design Skip List (LC 1206)** — KEEP. Canonical from-scratch implementation, thin wrapper delegating to the article's own `SkipList` class.
- **Range Sum Query with Frequent Insert/Delete** — KEEP. Requires span-augmentation for O(log n) rank queries — genuinely distinct from plain search/insert/delete, self-justified in the approach prose.
- **LRU-style Ordered Eviction by Score (Redis ZSET-style design)** — KEEP. Despite the "LRU-style" name suggesting overlap with `lru-cache.md`, this is actually score-based ordering (ZSET), not recency — the naming is a minor misnomer risk but the problem body is correctly distinct.

### Pass 2 — duplicate-problems citations

- Under entry 1: 1 citation — "Design a Sorted Set / Ordered Map from scratch" — **flagged as weak**: not a specific named/numbered problem, a generic category description. Not fabricated, but under-specified compared to the file's other citations. Recommend tightening to a concrete named problem.
- Under entry 2: **Count of Smaller Numbers After Self (LC 315)** — KEEP, verified (same LC 315 reused from red-black-tree.md's citation, independently re-evaluated fresh per the judgment-never-cached rule — valid here too). Honestly notes the more common accepted solution is a Fenwick tree, not a skip list.
- Under entry 3: **Design A Leaderboard (LC 1244)** — KEEP, verified, honest about when a simpler bisect approach would suffice.
- File-level: 3 entries, 1 citation each — each below the 3-5 advisory target individually, but reasonable given skip lists have fewer LC-native problems overall.

### Facts/correctness notes

- **Minor title mismatch**: entry 1's title says "Design Skip List (LeetCode 1206)"; LeetCode's actual title is "Design Skiplist" (one word). Number is correct, cosmetic text divergence only. `skip-list.md:253`.
- No complexity mismatches; entries 2-3 explicitly labeled as "sketch"/conceptual, an honest framing choice.

---

## `content/dsa/data-structures/stack.md`

### Pass 1 — full entries

- **Valid Parentheses** — KEEP. Bracket-matching/LIFO, exact topic fit. Code correct.
- **Daily Temperatures** — KEEP. Decreasing monotonic stack of indices. Code traced against the article's own stated output — exact match.
- **Min Stack** — KEEP. Auxiliary-stack running-extremum, distinct from the two monotonic-stack entries.
- **Evaluate Reverse Polish Notation** — KEEP. Operand-stack pop-two-apply-push, no monotonicity/matching/tracking. Traced — exact match.
- **Largest Rectangle in Histogram** — KEEP. Increasing monotonic stack with width computation. Traced with sentinel — exact match.

All 5 genuinely distinct. No REASSIGN/NEEDS-DECISION.

### Pass 2 — duplicate-problems citations

- Under Valid Parentheses: **Remove All Adjacent Duplicates (LC 1047)**, **Minimum Remove to Make Valid Parentheses (LC 1249)** — both KEEP, verified. Count: 2 — acceptable.
- Under Daily Temperatures: **Next Greater Element I (LC 496)** — KEEP, verified, consistent with `../patterns/monotonic-stack.md`'s own citation web (cross-checked, consistent). Count: 1 — justified (avoids redundant re-citation of problems already covered on the canonical pattern page).
- Under Min Stack: **Max Stack (LC 716)** — KEEP, verified. Count: 1 — justified, narrow natural pool.
- Under Evaluate RPN: zero citations — genuinely plausible no-close-duplicate case (Basic Calculator variants use a materially different mechanic, correctly not cited). A one-line "none found" note would be tidier but not a blocker.
- Under Largest Rectangle in Histogram: **Maximal Rectangle (LC 85)** — KEEP, consistent with cross-file citation web. Count: 1 — justified; Trapping Rain Water deliberately not re-cited here since `monotonic-stack.md` already covers that pairing.

### Facts/correctness notes

- No LC-number mismatches; all 4 citations verified.
- No code/approach inconsistencies across all 5 entries.

---

## `content/dsa/data-structures/string.md`

### Pass 1 — full entries

- **Valid Anagram** — KEEP. Character-count-array, distinct from all others.
- **Longest Substring Without Repeating Characters** — KEEP. Expanding/contracting window with last-seen-index map. Traced — matches.
- **Valid Palindrome** — KEEP. Converging two-pointer.
- **Find All Anagrams in a String** — KEEP. Fixed-width window + count-array match, distinct from entries 1 and 2.
- **Implement strStr / Find the Index** — KEEP. Rolling-hash (Rabin–Karp) search, only pattern-search entry.

**Topic-fit NEEDS-DECISION**: 3 of 5 entries (Longest Substring Without Repeating, Valid Palindrome, Find All Anagrams) are generic array/sequence techniques (sliding window, two pointers, fixed-window+count) that would solve identically over a plain array of chars — nothing exploits a string-specific mechanic. Only Valid Anagram (bounded-alphabet count-array) and strStr (string-matching-specific) lean on anything string-native. Tradeoff: (a) keep as-is — these are the expected canonical staples for a generically-titled "String" article, and the dedup plan's own prior audit of this file found zero distinct-technique violations, only this softer topic-fit concern; (b) swap 1-2 for something string-specific (KMP-lite, Z-function, trie-adjacent). Leaning (a) given the article's generic title and prior pass already accepted this shape, but flagging as a genuine judgment call.

### Pass 2 — duplicate-problems citations

**Systemic issue, all 5 entries**: every single `**Duplicate problems:**` citation in this file names the *same problem the entry itself solves*, pointing to a sister article's alternate treatment (`frequency-array.md`, `sliding-window.md`, `two-pointers.md`, `string-matching.md`) rather than a genuinely different problem sharing the technique. This is a miscategorization — the cross-links are factually accurate (those sister articles do solve the same named problem) but don't belong in a Duplicate-problems list, which requires a distinct problem.

- Valid Anagram: self-citation (LC 242) — **REMOVE**. **Ransom Note (LC 383)** — KEEP, genuine duplicate, verified.
- Longest Substring Without Repeating: self-citation (LC 3) — **REMOVE**. Zero genuine citations remain — under-researched; clean candidates exist (LC 340, LC 159, LC 904) and weren't used.
- Valid Palindrome: self-citation (LC 125) — **REMOVE**. Zero genuine remain — `two-pointers.md` itself already cites **Valid Palindrome II (LC 680)** as its own duplicate of this exact problem; a ready-made candidate this file failed to pull in.
- Find All Anagrams: self-citation (LC 438) — **REMOVE**. **Permutation in String (LC 567)** — KEEP, genuine duplicate, verified.
- Implement strStr: self-citation (LC 28) — **REMOVE**, and additionally a **technique mismatch**: the cross-linked target (`string-matching.md`) solves LC 28 via KMP, not this entry's Rabin-Karp — doubly wrong as a same-technique duplicate-problems citation. Zero genuine remain — real rolling-hash duplicates exist elsewhere in the codebase (`rabin-karp.md`'s own entries, e.g. Repeated DNA Sequences LC 187) and weren't cited here.

After removing all 5 self-citations, only 2 genuine duplicate-problem citations remain across the whole file (Ransom Note, Permutation in String) — 3 of 5 entries would have zero genuine citations. The file-level gated floor (≥1 entry with a genuine list) is still technically satisfied by the 2 remaining, but with a much thinner margin than the file appears to have at first read.

### Facts/correctness notes

- **This file's single biggest issue**: the systemic self-citation pattern above. No LC-number/title mismatches otherwise — every cited number matches its real title.
- No code/approach/complexity inconsistencies in any of the 5 entries.

---

## `content/dsa/data-structures/suffix-array.md`

### Pass 1 — full entries

- **Longest repeated substring** — KEEP. `max(LCP)` technique, foundational SA read-off. Traced on "banana" — matches.
- **Number of distinct substrings** — KEEP. `n(n+1)/2 - sum(LCP)` formula, distinct from entry 1 (sum vs max). Traced on "aab" — matches after hand-derivation.
- **Longest common substring of two strings** — KEEP. Cross-string LCP-with-boundary-check, requires the concatenation-with-separator trick absent from entries 1-2. Traced — matches.

**Cross-file note**: `suffix-tree.md`'s three entries solve the identical three named problems via the tree structure instead of the array — see NEEDS-DECISION under `suffix-tree.md` below. Doesn't change this file's own verdicts (its entries are correctly scoped natively), but the cross-file overlap needs a human call.

### Pass 2 — duplicate-problems citations

- Under Longest repeated substring: **"Longest Duplicate Substring" (LC 1044)** — this is actually **the same problem** (banana→"ana" is literally LC 1044's own example) miscategorized as a duplicate rather than the entry's own LC tag; **"Longest Repeated Non-Overlapping Substring"** — KEEP, genuinely a harder distinct variant.
- Under Number of distinct substrings: **"Count Different Palindromic Subsequences"** — **REMOVE**, the citation's own text admits "requires different structure," explicitly self-contradicting the purpose of a duplicate-problems list. **"Distinct Substrings" (SPOJ DISUBSTR)** — **REMOVE (self-citation)**, same problem under an alternate judge name. Zero genuine citations remain for this entry.
- Under Longest common substring: **"Longest Common Substring" (SPOJ LCS)** — **REMOVE (self-citation)**. **"...sequence-alignment tasks"** — **REMOVE**, vague hand-waving, no verifiable identity. **"...generalized suffix array problems (k strings)"** — too vague as written; should instead directly name `suffix-tree.md`'s own k-strings entry (which already correctly cross-links back here). Zero genuine, verifiable citations remain.

**Format note (all three entries)**: every Duplicate-problems line is written as inline prose rather than the required one-bullet-per-line format — a straightforward format fix independent of the content issues.

### Facts/correctness notes

- All three entries' code hand-traced against worked examples — no logic errors.
- Complexity claims correctly hedge on SA-construction variant used, no overclaiming.
- **Systemic issue mirroring string.md, more severe**: of 6 individual citations across 3 entries, only 1 (Longest Repeated Non-Overlapping Substring) is unambiguously genuine. The rest are self-citations under alternate judge names or vague/self-contradicting prose.

---

## `content/dsa/data-structures/suffix-tree.md`

### Pass 1 — full entries

- **Longest Repeated Substring (via suffix tree)** — **NEEDS-DECISION**. Mechanically distinct traversal (tree DFS over explicit nodes vs linear LCP-array scan) but reads off the exact same underlying fact as `suffix-array.md`'s entry 1 — the two representations are classically interconvertible. The article's own prose explicitly owns the overlap ("the tree-native version... same answer, different structure to read it off of"). Tradeoff: (a) KEEP as distinct — traversal mechanic genuinely differs, and per-article distinctness scoping (dsa-writer.md U6: "do not cross-check against other articles' problem selection") may mean this isn't even a rule violation; (b) REASSIGN — collapse per the dedup plan's own "same core invariant/mechanic regardless of surface framing" precedent language, demoting the tree version to a duplicate-problems line. The *volume* of overlap (all 3 of 3 entries mirror all 3 of 3 in suffix-array.md) is unusual enough to warrant a human call rather than a forced verdict.
- **Count Distinct Substrings (via suffix tree)** — same NEEDS-DECISION reasoning; article's prose again explicitly owns the parallel ("both count the same thing, from opposite ends").
- **Longest Common Substring Across k Strings (Generalized Suffix Tree)** — KEEP, clean and well-justified. The article correctly argues the tree earns its keep here specifically — the 2-string separator trick "doesn't generalize cleanly past two strings," making this a genuine technique upgrade, not just a restructuring. Best example in the file of doing cross-file distinction correctly.

### Pass 2 — duplicate-problems citations

- Under entry 1: cross-references `suffix-array.md`'s own version — KEEP conditional on the Pass-1 NEEDS-DECISION resolving toward "distinct" (moot if collapsed). **Longest Duplicate Substring (LC 1044)** — KEEP, verified, well-hedged citation.
- Under entry 2: cross-references `suffix-array.md`'s entry 2 — same conditional logic. **"Count of Distinct Substrings of Length K"** — borderline KEEP, unverifiable as a specific named problem but honestly framed as a variant, not dressed up as verified.
- Under entry 3: **"Longest Common Substring (Suffix Array version)"** — KEEP, correctly framed as "the k=2 special case," an honest specialization relationship. **"Shortest Common Superstring"** — **REMOVE recommended**: the citation's own text says "a related but distinct problem — requires a different technique," which by its own admission doesn't belong in a Duplicate-problems list (same self-contradiction pattern as `suffix-array.md`'s Palindromic-Subsequences citation).

### Facts/correctness notes

- All three entries' code hand-traced against worked examples — no logic errors, including the subtle sentinel-only-path `-1` adjustment in entry 2.
- Complexity claims honest about the naive-vs-Ukkonen construction gap — good practice, no overclaiming.

---

## `content/dsa/data-structures/treap.md`

### Pass 1 — full entries

- **Design a Sorted Set with Fast Rank Queries** — KEEP. Augmented-size-field rank-query, genuinely exercises the treap's core value proposition (random-priority balancing under adversarial insertion order). Code hand-traced — matches.
- **Range Reverse and Query (Implicit Treap)** — KEEP, but with a **hard structural gap**: **missing Solution code block entirely**. Problem/Worked-examples/Constraints/Approach are all present and well-written, but the entry jumps straight from Approach to Complexity with no `python` code block at all. This is a U6 violation, not a judgment call — every other entry in this chunk (5 files, 19 entries) has code; this is the only structural gap found.
- **Merge Two Treaps / Union of Two Sorted Sets** — KEEP. Whole-treap `merge` primitive, distinct from entries 1-2. Code correct, complexity correctly includes the recursive-space term.

### Pass 2 — duplicate-problems citations

- Under entry 1: **Order-Statistics Tree (k-th smallest)** (no LC number, classic/textbook, correctly allowed) — KEEP. **Count of Smaller Numbers After Self (LC 315)** — KEEP, verified. Count: 2 — acceptable.
- Under entry 2 (Range Reverse): **"Reverse Substring range-update variants... (Codeforces)"** — **REMOVE, unverifiable**: no specific problem ID, no findable match via WebSearch — reads as a plausible-sounding placeholder, not a real citable problem. **"Rope data structure operations"** — borderline KEEP as a technique reference (factually solid, ropes genuinely are implemented via implicit balanced trees) but doesn't fit the "Problem Name (LC NNN if exists)" shape — describes an application domain, not a solvable problem. Compounded by the missing code (Pass 1), making this the weakest entry in the file.
- Under entry 3: **Union of Two Balanced BSTs (weight-balanced tree "join")** — KEEP, correctly framed as a harder cousin. **Persistent Treap Version Merge** — KEEP, genuine documented extension. Count: 2 — acceptable, both correctly un-tagged per decision 8.

### Facts/correctness notes

- **Entry 2's missing Solution code block** — flagged above, needs to be filled before this entry would pass a rater pass (U6 is gated, V2 code-correctness can't even be checked without code).
- No fabricated LC numbers elsewhere in the file; all other citations correctly omit LC tags per decision 8 rather than inventing one.
- Complexity claims correctly state expected (not worst-case) bounds, correctly include recursive-space terms.

---

## `content/dsa/data-structures/trie.md`

### Pass 1 — full entries

- **Implement a Trie** — KEEP. Foundational API-surface entry (exact match / prefix existence / insert), distinct from every other entry.
- **Word Search II (LC 212)** — KEEP. Shared trie + grid-DFS-with-pruning, distinct branch driver (spatial neighbors) from entry 5's wildcard fan-out.
- **Replace Words (LC 648)** — KEEP. Walk-to-first-`is_end` (shortest-prefix-wins), distinct from entries 1 and 2.
- **Maximum XOR of Two Numbers** — KEEP. Binary trie over bit-strings + greedy opposite-bit walk, structurally unlike any string-trie entry. Code matches the article's own CP-primitives `BitTrie` class.
- **Design Add and Search Words (LC 211)** — KEEP. Trie + wildcard-driven backtracking DFS, distinct branch driver from entry 2.

All 5 genuinely distinct. No REASSIGN/NEEDS-DECISION.

### Pass 2 — duplicate-problems citations

- Under Implement a Trie: **Map Sum Pairs (LC 677)**, **Longest Word in Dictionary (LC 720)** — both KEEP, verified. Count: 2 — acceptable.
- Under Word Search II: zero citations — defensible (fairly singular multi-word+grid+backtracking+pruning combination), but flagged as borderline since 0-vs-"at least try" is a judgment call.
- Under Replace Words: zero citations — acceptable, no obvious duplicate found, matches the "don't force a bad dup-line" precedent.
- Under Maximum XOR: **Maximum XOR With an Element From Array (LC 1707)** — KEEP, verified. Count: 1 — acceptable, narrow specialized technique.
- Under Design Add and Search Words: zero citations — the third entry with zero coverage in this file.

**File-level**: only 2 of 5 entries carry citations. Cross-reference: the dedup plan's own history records this file as one of 8 originally-zero-dup-line files fixed in a prior pass (went from 0 to 2 lists) — progress, but still short of the 3-5 advisory target.

### Facts/correctness notes

- **Missing LC-number tag**: "Maximum XOR of Two Numbers" is exactly LC 421 (confirmed — worked example matches verbatim) but the heading omits the tag, inconsistent with the entry's own duplicate-problems bullet one line below, which does cite an LC number.
- No pseudocode/code mismatches, no off-by-ones, no complexity misstatements across any of the 5 entries.

---

## `content/dsa/data-structures/union-find.md`

### Pass 1 — full entries

- **Number of Connected Components** — KEEP. Canonical template DSU, decremental component counter.
- **Kruskal's MST** — KEEP. Sort-edges + DSU-as-cycle-guard, distinct objective (MST-optimization-with-sort) from entry 4.
- **Accounts Merge** — KEEP. DSU keyed on strings via implicit shared attribute, distinct from integer-indexed entries and from entry 5's different input shape.
- **Redundant Connection (LC 684)** — KEEP. Per-edge cycle gate in input order, pure detection with no sorting/optimization — genuinely distinct objective from Kruskal despite both using "check before union."
- **Satisfiability of Equality Equations** — KEEP. Two-pass equality-constraint propagation, fixed 26-element domain, distinct build-then-validate structure. Path-halving compression variant in `find` hand-verified correct (differs from other entries' full recursive compression, but a valid textbook variant).

No REASSIGN calls — entries 2/4 were the closest pair but the objective-function difference (optimization-with-sort vs pure-detection-in-order) is a real technique difference, consistent with how the dedup plan's own Algorithms audit treated similar close calls.

### Pass 2 — duplicate-problems citations

- Under entry 1: **Number of Provinces (LC 547)**, **Number of Islands (LC 200)** — both KEEP, verified. Count: 2 — acceptable.
- Under entry 2: **Min Cost to Connect All Points (LC 1584)**, **Connecting Cities With Minimum Cost (LC 1135)** — both KEEP, verified. Count: 2 — acceptable.
- Under entry 3: **Sentence Similarity II (LC 737)**, **Largest Component Size by Common Factor (LC 952)** — both KEEP, verified, precisely reasoned. Count: 2 — acceptable.
- Under entry 4: **Redundant Connection II (LC 685)**, **Graph Valid Tree (LC 261)** — both KEEP, verified. Count: 2 — acceptable.
- Under entry 5: zero citations — acceptable, genuinely singular technique, no forced citation found (LC 947 correctly ruled out as a different mechanic).

**File-level**: 4 of 5 entries have exactly 2 citations each — best-covered file in this run.

### Facts/correctness notes

- **Missing LC-number tag**: entry 5 ("Satisfiability of Equality Equations") is exactly LC 990 (confirmed, worked example matches verbatim) but the heading omits the tag, inconsistent with the file's own convention on entries 1-4.
- **Redundant/duplicated prose** at `union-find.md:277` (CP-primitives, "Offline connectivity with rollback"): the O(log²n) complexity derivation is stated twice, near-verbatim, within the same bullet — a copy-paste/editing artifact, not a math error (both statements agree and are individually correct). Should be deduplicated to one sentence.
- No off-by-one or logic errors in any of the 5 entries.

---

## Summary

- **Files audited**: 26 (all real, non-stub Data Structures files with a `## Practice problems` section — `circular-buffer.md`, `hash-set.md`, `segment-tree.md` skipped as stubs).
- **Full entries**: KEEP 118 / REASSIGN 0 (1 lean-REASSIGN flagged as NEEDS-DECISION) / NEEDS-DECISION 6 (`bloom-filter.md` entries 1/4 pairing, `red-black-tree.md` entry 3's non-runnable code, `linked-list.md` entry 5 LRU Cache, `b-tree.md`'s zero-citation gap, `suffix-tree.md` entries 1 and 2 vs `suffix-array.md`).
- **Duplicate citations**: KEEP ~140 / REMOVE 13 (5 self-citations in `string.md`, 4 in `suffix-array.md`, 2 in `suffix-tree.md`/`treap.md`, 1 hard-mismatch LC number in `b-plus-tree.md`) / PROMOTE candidates 0 (no citation was judged strong enough to warrant full-entry promotion).
- **Facts/correctness issues found**: 2 title-text mismatches (`binary-search-tree.md` LC 1650, `skip-list.md` LC 1206), 1 hard LC-number mismatch (`b-plus-tree.md` LC 2250), 6 missing-LC-tag omissions on entries that are exact canonical matches (`heap.md` ×2, `lfu-cache.md`, `trie.md`, `union-find.md`, `dynamic-array.md`'s own entries clean), 1 non-runnable/skeleton code block (`lfu-cache.md` entry 2), 1 missing Solution code block entirely (`treap.md` entry 2), 1 duplicated-prose artifact (`union-find.md`), 1 claimed-vs-actual discrepancy in prior fix history (`hash-table.md`).

**Most impactful findings** (highest-priority items to work through):

1. `b-plus-tree.md` — duplicate-problems citation cites the wrong real LC problem (LC 2250 is unrelated geometry, not range-counting) — hard fail, needs a fix or removal.
2. `treap.md` entry 2 — missing Solution code block entirely, a hard U6 structural gap blocking any rater pass.
3. `string.md` — systemic self-citation pattern across all 5 entries; after cleanup only 2 genuine duplicate-problems citations remain file-wide.
4. `suffix-array.md` — even more severe version of the same self-citation pattern; only 1 of 6 citations is genuinely valid.
5. `linked-list.md` entry 5 vs `lru-cache.md` entry 1 — near-identical full "LRU Cache" entry duplicated across 2-3 files; needs a human reconciliation call.
6. `hash-table.md` — discrepancy between the dedup plan's claimed "all files fixed" completion state and the file's actual current content (3 of 5 entries still have zero dup-lines and no justification note).
7. `bloom-filter.md` entries 1 and 4 — same core sizing math/code shape under different word-problems; closest true collision-candidate pair found in this run.
8. `red-black-tree.md` entry 3 and `lfu-cache.md` entry 2 — both ship non-self-contained/skeleton code instead of full runnable solutions.
9. `suffix-tree.md` — all 3 entries structurally mirror `suffix-array.md`'s all 3 entries on the same named problems; the volume of overlap (not any single pair) is what's flagged.
10. Widespread missing-LC-number-tag pattern on entries that are exact, verified canonical matches (`heap.md`, `lfu-cache.md`, `trie.md`, `union-find.md`) — low severity individually, but a recurring, easily-fixed consistency gap.

Full detail for every file is above; this summary surfaces only the highest-priority items.
