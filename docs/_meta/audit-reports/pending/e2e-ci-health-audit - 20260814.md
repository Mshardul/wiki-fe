# E2E CI Health Audit (wiki-fe)

Generated by e2e-ci-health audit agent. Checks drift against `docs/_meta/plans/e2e-ci-resource-hardening.md` - not a test-content quality audit or a security audit.

Test count this run: 814 (prior run: N/A - first audit run, no prior report exists)

## Findings by check

### Lane A/Lane B classification drift

No findings. Confirmed exactly 5 `@pytest.mark.heavy` tests, all matching the canvas/PNG/download criteria (`page.expect_download()`), across `test_content_enhancements.py:3553,3572`, `test_data_backup.py:24,39`, `test_html_markup.py:162` - identical to the plan's confirmed list. `pytest.ini` marker registration intact with description text matching plan intent. CI still filters on it: `tests-light` runs `-m "not heavy"` (809 collected), `tests-heavy` runs `-m heavy` (5 collected) - exact partition, no overlap/gap (809 + 5 = 814).

### Worker/resource configuration regression

No findings. `.github/workflows/ci.yml` uses bounded `-n 2` per shard for `tests-light` (3 file-based shards, matrix-defined) and `-n 1` for `tests-heavy` - no bare `-n auto` anywhere. `xdist_group`/`--dist loadgroup` absent from the codebase entirely, matching the plan's explicit Decision #3 finding that it's redundant with single-worker serial heavy lane. Playwright browser cache step (`actions/cache@v4`, keyed on `hashFiles('requirements-dev.txt')`, path `~/.cache/ms-playwright`) present ahead of `Install Chromium` in both `tests-light` and `tests-heavy` jobs - not regressed to uncached.

- **Check:** worker-config-regression
- **Files:** `requirements-dev.txt`
- **Observation:** `pytest-rerunfailures>=14.0` is present in `requirements-dev.txt`, but no `--reruns` flag (or any rerun config) appears anywhere in `.github/workflows/ci.yml`, `pytest.ini`, or `tests/conftest.py` - the plugin is installed but inert, not wired into any test invocation.
- **Baseline (from plan doc):** Decision #5, lever #5 status was "NOT STARTED... lowest priority - only add once 1-2 [other levers] are proven on real CI." The plan doc's own dependency list (`requirements-dev.txt` cat output) already shows `pytest-rerunfailures>=14.0` as an existing baseline dependency, not something this audit is catching newly added.
- **Drift:** none - this matches the plan's recorded baseline exactly (dependency present, lever not implemented). Documenting for completeness since Check 2 explicitly asks to verify rerun/retry plugin scope.
- **Fix direction:** none needed. If the plugin is ever wired in, confirm at that time it's scoped to worker-crash-type failures only (e.g. via a custom `--reruns-condition` or marker-based scoping), not blanket assertion retries.

### New test growth against the resource budget

- **Check:** test-growth
- **Files:** `tests/e2e/*.py`
- **Observation:** No prior `e2e-ci-health-audit - *.md` report exists in `docs/_meta/audit-reports/` - this is the first run of this audit. Current collected total: 814 tests (809 Lane A + 5 Lane B).
- **Baseline (from plan doc):** Plan doc's last recorded count (2026-08-03, Decision #1 implementation) was 773 tests (768 Lane A + 5 Lane B), verified via local `--collect-only`.
- **Drift:** +41 tests since the plan's last recorded snapshot (773 → 814), all in Lane A (Lane B held steady at 5). No fixed growth threshold to enforce per plan scope - reporting the trend for human judgment, per Check 3 instructions.
- **Fix direction:** none required by this audit. Future audits should compare against this report's 814 baseline for the next delta.

### B2 sequencing pattern conformance

No findings for `test_content_enhancements.py` - all 4 previously-implemented chained groups still present and intact: highlight lifecycle (create → keyboard-remove via popover, `test_keyboard_enter_removes_focused_highlight`), marker lifecycle (create → remove via popover), highlight reanchor/drop (`test_highlight_reanchor_and_drop_on_upstream_edit`, two-phase upstream-edit test), save-as-card presence (`test_save_as_card_button_visible_and_labeled`, chained visibility + aria-label check). The two heavy download tests remain correctly unmerged with light tests, preserving Lane B isolation per plan intent.

- **Check:** b2-sequencing
- **Files:** `tests/e2e/test_read_toggle.py`, `tests/e2e/test_bookmarks.py`, `tests/e2e/test_recents.py`, `tests/e2e/test_index_ux.py`, `tests/e2e/test_offline_shelf.py`, `tests/e2e/test_scroll_toc.py`
- **Observation:** Sampled 6 of the 17 files the plan's own prior audit (2026-08-03, not yet implemented) identified as having genuine chaining candidates. All 6 remain unchained - e.g. `test_read_toggle.py` still has `test_clicking_completion_btn_marks_article_completed` / `test_completion_btn_shows_done_state_after_click` / `test_clicking_completion_btn_again_marks_uncompleted` as separate functions each redoing setup; `test_bookmarks.py` still has separate bookmark→index→modal→remove tests; `test_offline_shelf.py` still has separate seed/evict tests.
- **Baseline (from plan doc):** Decision #4 explicitly states "rest of the suite (audited, NOT YET implemented)" covering 17 groups/47 tests across 32 files including these.
- **Drift:** none - matches the plan's own recorded "not yet implemented" status exactly. Not re-flagging as new findings since the plan already accounts for this as pending work, not drift.
- **Fix direction:** none from this audit; implementation of the already-identified 17 groups remains open per the plan, tracked there rather than here.

No new unchained boilerplate patterns found beyond what the plan's own prior audit already catalogued in the 6 sampled files.

## Raw log
(empty once fully organized)
