# Terms

Consistent hashing spreads keys evenly [?assuming a good hash function] across nodes.

A <abbr title="Least Recently Used">LRU</abbr> cache evicts the oldest entry.

Plain <abbr title="Not in glossary">XYZ</abbr> stays untouched.
