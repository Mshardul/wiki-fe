[pytest]
markers =
    smoke: fast subset covering the most critical paths (run with -m smoke)
    slow: tests that are inherently slow (network waits, timers, reloads)
    heavy: canvas/PNG rasterization or file-download tests, isolated in CI to avoid xdist worker crashes under resource contention

timeout = 30
timeout_method = thread
